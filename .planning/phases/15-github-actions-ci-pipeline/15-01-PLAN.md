---
phase: 15-github-actions-ci-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - Dockerfile
  - README.md
autonomous: true
requirements:
  - CI-05
  - CI-08
  - CI-09

must_haves:
  truths:
    - "Running `mvn test -q` generates target/site/jacoco/jacoco.xml after this plan"
    - "The Dockerfile contains the OCI LABEL required for GHCR package linking"
    - "README.md shows a CI workflow badge after the main heading"
  artifacts:
    - path: "pom.xml"
      provides: "JaCoCo plugin configuration (prepare-agent, report, check)"
      contains: "jacoco-maven-plugin"
    - path: "Dockerfile"
      provides: "OCI image source label required for GHCR push"
      contains: "org.opencontainers.image.source"
    - path: "README.md"
      provides: "CI badge linking to the workflow run page"
      contains: "ci.yml/badge.svg"
  key_links:
    - from: "pom.xml jacoco-maven-plugin"
      to: "target/site/jacoco/jacoco.xml"
      via: "mvn test → jacoco:report goal at test phase"
      pattern: "jacoco-report"
    - from: "Dockerfile LABEL"
      to: "GHCR repository link"
      via: "docker push → GHCR package association"
      pattern: "org.opencontainers.image.source"
---

<objective>
Add the three file-level prerequisites that the CI pipeline depends on:
1. JaCoCo Maven plugin in pom.xml — generates target/site/jacoco/jacoco.xml which the madraphos/jacoco-report action (CI-08) reads for PR comments
2. OCI LABEL in Dockerfile — links the container package to the GitHub repository on first GHCR push (CI-05 prerequisite)
3. CI badge in README.md — CI-09

Purpose: Without these three changes, the Wave 2 ci.yml would either silently fail (no jacoco.xml for coverage comment, no OCI label causes GHCR permission_denied) or be incomplete (no badge). These are fast, surgical edits to existing files that can be locally verified with `mvn test -q` before the workflow YAML is written.

Output: Modified pom.xml, Dockerfile, and README.md. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-github-actions-ci-pipeline/15-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add JaCoCo plugin to pom.xml and OCI LABEL to Dockerfile</name>

  <read_first>
    - pom.xml — read the full file to locate the &lt;build&gt;&lt;plugins&gt; section; insert the JaCoCo plugin after the maven-failsafe-plugin block; confirm argLine property exists (it does: &lt;argLine/&gt; at line 25)
    - Dockerfile — read the full file to locate the EXPOSE 8080 instruction; insert the LABEL line immediately before it
    - .planning/phases/15-github-actions-ci-pipeline/15-RESEARCH.md — use the exact XML block from "JaCoCo Plugin for pom.xml" section verbatim; use the OCI label from "OCI Label for Dockerfile (D-11)" section verbatim
  </read_first>

  <files>pom.xml, Dockerfile</files>

  <action>
**pom.xml — insert JaCoCo plugin:**

Add the following plugin block inside &lt;build&gt;&lt;plugins&gt;, after the closing &lt;/plugin&gt; tag of the maven-failsafe-plugin (which ends at approximately line 236). Do NOT place it before Surefire or Failsafe — JaCoCo's prepare-agent must bind at initialize phase which runs before the Surefire test phase:

```xml
      <!-- JaCoCo Maven Plugin — generates coverage report consumed by CI-08 (madraphos/jacoco-report)
           Version must be explicit — Spring Boot 2.6.15 BOM does not manage jacoco-maven-plugin -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>jacoco-prepare-agent</id>
            <phase>initialize</phase>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>jacoco-report</id>
            <phase>test</phase>
            <goals><goal>report</goal></goals>
            <configuration>
              <excludes>
                <exclude>com/aflokkat/dto/**</exclude>
                <exclude>com/aflokkat/entity/**</exclude>
                <exclude>com/aflokkat/aggregation/**</exclude>
                <exclude>com/aflokkat/domain/**</exclude>
              </excludes>
            </configuration>
          </execution>
          <execution>
            <id>jacoco-check</id>
            <phase>test</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <excludes>
                <exclude>com/aflokkat/dto/**</exclude>
                <exclude>com/aflokkat/entity/**</exclude>
                <exclude>com/aflokkat/aggregation/**</exclude>
                <exclude>com/aflokkat/domain/**</exclude>
              </excludes>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <!-- Measured baseline from Phase 12 on prior branch: 43%
                           Threshold set at 38% (baseline minus 5%) to tolerate minor drift -->
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.38</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
```

**Exclusion paths explanation:** Package is `com.aflokkat` (not `com.st4r4x` — that was the old package). Path format for JaCoCo exclusions uses forward slashes with wildcard `**`. The four excluded packages are DTOs, JPA entities, aggregation POJOs, and domain POJOs — pure data holders with no business logic.

**Dockerfile — insert OCI LABEL (per D-11):**

In the runtime stage (second FROM block), add the LABEL immediately before the `EXPOSE 8080` line. Current Dockerfile has `EXPOSE 8080` at line 32. Insert:

```dockerfile
LABEL org.opencontainers.image.source=https://github.com/St4r4x/restaurant-analytics
```

The block around the insertion point should look like:

```dockerfile
# MongoDB connection defaults (overridden by docker-compose environment section)
ENV MONGODB_URI=mongodb://mongodb:27017
ENV MONGODB_DATABASE=newyork
ENV MONGODB_COLLECTION=restaurants

LABEL org.opencontainers.image.source=https://github.com/St4r4x/restaurant-analytics

# Expose port
EXPOSE 8080
```

This label is required for GHCR to link the container package to the `St4r4x/restaurant-analytics` repository and grant the workflow's `GITHUB_TOKEN` push access on first push. Without it, `docker push ghcr.io/st4r4x/restaurant-analytics` returns `denied: permission_denied`.
  </action>

  <verify>
    <automated>cd /home/missia03/Projects/restaurant-analytics && mvn test -q 2>&1 | tail -5 && test -f target/site/jacoco/jacoco.xml && echo "jacoco.xml EXISTS" || echo "jacoco.xml MISSING"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "jacoco-maven-plugin" pom.xml` returns at least 1
    - `grep -c "jacoco-prepare-agent" pom.xml` returns 1
    - `grep -c "jacoco-report" pom.xml` returns at least 1 (execution id)
    - `grep -c "jacoco-check" pom.xml` returns 1
    - `grep "minimum" pom.xml` shows `0.38`
    - `grep "com/aflokkat/dto" pom.xml` shows the exclusion (not com/st4r4x)
    - `grep "org.opencontainers.image.source" Dockerfile` returns exactly one line
    - The LABEL line appears before `EXPOSE 8080` in Dockerfile (grep -n both and compare line numbers)
    - After `mvn test -q`: `test -f target/site/jacoco/jacoco.xml` exits 0
    - `mvn test -q` exits with code 0 (all existing tests still pass, coverage threshold met)
  </acceptance_criteria>

  <done>pom.xml has the JaCoCo plugin with prepare-agent, report, and check executions using com/aflokkat/ exclusion paths. Dockerfile has the OCI LABEL before EXPOSE. `mvn test -q` passes and creates target/site/jacoco/jacoco.xml.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add CI badge to README.md</name>

  <read_first>
    - README.md — read the first 10 lines to identify the exact heading text and line number for badge insertion; the heading is `# Restaurant Analytics — NYC Inspection Data` on line 1
    - .planning/phases/15-github-actions-ci-pipeline/15-CONTEXT.md — D-17 for exact badge URL format
  </read_first>

  <files>README.md</files>

  <action>
Insert the CI badge on the line immediately after the `# Restaurant Analytics — NYC Inspection Data` heading (line 1). The badge must be on its own line, followed by a blank line before the description paragraph.

The line to insert (per D-17, exact format):

```markdown
![CI](https://github.com/St4r4x/restaurant-analytics/actions/workflows/ci.yml/badge.svg)
```

After the edit, lines 1–4 of README.md should look like:

```markdown
# Restaurant Analytics — NYC Inspection Data

![CI](https://github.com/St4r4x/restaurant-analytics/actions/workflows/ci.yml/badge.svg)

Spring Boot REST API and web dashboard for exploring NYC restaurant hygiene data.
```

Keep all existing README content unchanged below this insertion.
  </action>

  <verify>
    <automated>grep -n "ci.yml/badge.svg" /home/missia03/Projects/restaurant-analytics/README.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "ci.yml/badge.svg" README.md` returns exactly one line containing `![CI](https://github.com/St4r4x/restaurant-analytics/actions/workflows/ci.yml/badge.svg)`
    - The badge URL uses `St4r4x` (capital S, capital r, digit 4, lowercase x) — exact case matters for GitHub Actions URL routing
    - The badge line appears within the first 5 lines of README.md (`head -5 README.md | grep badge` returns a match)
    - All content below the badge is unchanged (spot-check: `grep "Spring Boot REST API" README.md` still returns the description line)
  </acceptance_criteria>

  <done>README.md has the CI badge on line 3 (after the heading and a blank line), pointing to the ci.yml workflow on the main branch. The badge will show a green checkmark once the workflow runs successfully on main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pom.xml → JaCoCo check goal | Coverage threshold is a build gate; must not be set so high that it blocks all future work |
| Dockerfile LABEL → GHCR | OCI label grants the workflow's GITHUB_TOKEN the right to push to this specific repository's package registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Information Disclosure | pom.xml jacoco-check minimum | accept | Threshold (0.38) is a build-quality gate, not a secret. Acceptable to be in source. |
| T-15-01-02 | Elevation of Privilege | Dockerfile LABEL org.opencontainers.image.source | mitigate | Label must reference the exact repository URL (`St4r4x/restaurant-analytics`) to prevent GHCR from linking the package to an unrelated repository. Verify with `grep "org.opencontainers.image.source=https://github.com/St4r4x/restaurant-analytics" Dockerfile` returns exactly this string. |
| T-15-01-03 | Tampering | README.md badge URL | accept | Badge URL is public information; it cannot be used to forge CI status as GitHub Actions generates the badge server-side from actual run results. |
</threat_model>

<verification>
After both tasks:

1. `mvn test -q` exits 0 — all existing tests pass with JaCoCo plugin present
2. `test -f target/site/jacoco/jacoco.xml` exits 0 — coverage XML generated
3. `grep "jacoco-maven-plugin" pom.xml` returns a match
4. `grep "org.opencontainers.image.source" Dockerfile` returns exactly one match before `EXPOSE 8080`
5. `grep "ci.yml/badge.svg" README.md` returns one match in the first 5 lines
6. `grep "com/st4r4x" pom.xml` returns zero matches (exclusions use correct com/aflokkat/ paths)
</verification>

<success_criteria>
- JaCoCo plugin (0.8.12) added to pom.xml with prepare-agent (initialize), report (test), and check (test) executions
- JaCoCo exclusions reference `com/aflokkat/` paths (not the old `com/st4r4x/` paths)
- Coverage threshold set to `0.38` (38%) with comment explaining Phase 12 baseline
- `mvn test -q` completes green and produces `target/site/jacoco/jacoco.xml`
- Dockerfile has `LABEL org.opencontainers.image.source=https://github.com/St4r4x/restaurant-analytics` immediately before `EXPOSE 8080`
- README.md badge line: `![CI](https://github.com/St4r4x/restaurant-analytics/actions/workflows/ci.yml/badge.svg)` within first 5 lines
</success_criteria>

<output>
After completion, create `.planning/phases/15-github-actions-ci-pipeline/15-01-SUMMARY.md` with:
- What was added to pom.xml (plugin version, execution IDs, threshold value, exclusion paths)
- What was added to Dockerfile (exact LABEL line and its position)
- What was added to README.md (badge line and its position)
- Actual `mvn test -q` exit code and confirmation that jacoco.xml was generated
- Any deviation from the plan (e.g., if the threshold needed adjustment because existing coverage is below 38%)
</output>
