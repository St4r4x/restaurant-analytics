---
phase: 21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/main/java/com/st4r4x/config/SecurityConfig.java
  - src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java
  - src/main/java/com/st4r4x/security/RateLimitFilter.java
  - src/main/java/com/st4r4x/entity/UserEntity.java
  - src/main/java/com/st4r4x/entity/BookmarkEntity.java
  - src/main/java/com/st4r4x/entity/InspectionReportEntity.java
  - src/test/java/com/st4r4x/config/SecurityConfigTest.java
autonomous: true
requirements: [UPGRADE-02]

must_haves:
  truths:
    - "No javax.servlet.* or javax.persistence.* imports remain in any source file"
    - "All 7 affected files compile against jakarta.* namespace"
    - "mvn clean compile exits 0 after this plan"
  artifacts:
    - path: "src/main/java/com/st4r4x/config/SecurityConfig.java"
      provides: "jakarta.servlet.http.HttpServletResponse import"
      contains: "jakarta.servlet.http.HttpServletResponse"
    - path: "src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java"
      provides: "jakarta.servlet.* imports"
      contains: "jakarta.servlet.FilterChain"
    - path: "src/main/java/com/st4r4x/security/RateLimitFilter.java"
      provides: "jakarta.servlet.* imports"
      contains: "jakarta.servlet.FilterChain"
    - path: "src/main/java/com/st4r4x/entity/UserEntity.java"
      provides: "jakarta.persistence.* imports"
      contains: "jakarta.persistence.Entity"
    - path: "src/main/java/com/st4r4x/entity/BookmarkEntity.java"
      provides: "jakarta.persistence.* imports"
      contains: "jakarta.persistence.Entity"
    - path: "src/main/java/com/st4r4x/entity/InspectionReportEntity.java"
      provides: "jakarta.persistence.* imports"
      contains: "jakarta.persistence.Entity"
    - path: "src/test/java/com/st4r4x/config/SecurityConfigTest.java"
      provides: "jakarta.servlet.Filter import"
      contains: "jakarta.servlet.Filter"
  key_links:
    - from: "javax.servlet"
      to: "jakarta.servlet"
      via: "Spring Boot 3+/4+ Jakarta EE 10"
      pattern: "import jakarta.servlet"
    - from: "javax.persistence"
      to: "jakarta.persistence"
      via: "Hibernate 6 / Jakarta Persistence 3.x"
      pattern: "import jakarta.persistence"
---

<objective>
Rename all javax.* imports to jakarta.* across 6 main source files and 1 test file. This is a pure namespace migration — no annotation semantics change. After this plan, `mvn clean compile` must succeed.

Purpose: Spring Boot 3+ uses Jakarta EE 10 exclusively. The javax.* namespace was the single source of all compilation errors introduced by Plan 01's Boot version bump.
Output: 7 files with updated imports. `mvn clean compile` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju/21-RESEARCH.md
</context>

<interfaces>
<!-- Exact javax imports to replace per file. Executor: do ONLY these replacements. -->

SecurityConfig.java — 1 import:
  FROM: import javax.servlet.http.HttpServletResponse;
  TO:   import jakarta.servlet.http.HttpServletResponse;

JwtAuthenticationFilter.java — 4 imports:
  FROM: import javax.servlet.FilterChain;
  TO:   import jakarta.servlet.FilterChain;
  FROM: import javax.servlet.ServletException;
  TO:   import jakarta.servlet.ServletException;
  FROM: import javax.servlet.http.HttpServletRequest;
  TO:   import jakarta.servlet.http.HttpServletRequest;
  FROM: import javax.servlet.http.HttpServletResponse;
  TO:   import jakarta.servlet.http.HttpServletResponse;

RateLimitFilter.java — 4 imports:
  FROM: import javax.servlet.FilterChain;
  TO:   import jakarta.servlet.FilterChain;
  FROM: import javax.servlet.ServletException;
  TO:   import jakarta.servlet.ServletException;
  FROM: import javax.servlet.http.HttpServletRequest;
  TO:   import jakarta.servlet.http.HttpServletRequest;
  FROM: import javax.servlet.http.HttpServletResponse;
  TO:   import jakarta.servlet.http.HttpServletResponse;

UserEntity.java — 6 imports:
  FROM: import javax.persistence.Column;
  TO:   import jakarta.persistence.Column;
  FROM: import javax.persistence.Entity;
  TO:   import jakarta.persistence.Entity;
  FROM: import javax.persistence.GeneratedValue;
  TO:   import jakarta.persistence.GeneratedValue;
  FROM: import javax.persistence.GenerationType;
  TO:   import jakarta.persistence.GenerationType;
  FROM: import javax.persistence.Id;
  TO:   import jakarta.persistence.Id;
  FROM: import javax.persistence.Table;
  TO:   import jakarta.persistence.Table;

BookmarkEntity.java — 8 imports:
  FROM: import javax.persistence.Column;
  TO:   import jakarta.persistence.Column;
  FROM: import javax.persistence.Entity;
  TO:   import jakarta.persistence.Entity;
  FROM: import javax.persistence.FetchType;
  TO:   import jakarta.persistence.FetchType;
  FROM: import javax.persistence.GeneratedValue;
  TO:   import jakarta.persistence.GeneratedValue;
  FROM: import javax.persistence.GenerationType;
  TO:   import jakarta.persistence.GenerationType;
  FROM: import javax.persistence.Id;
  TO:   import jakarta.persistence.Id;
  FROM: import javax.persistence.JoinColumn;
  TO:   import jakarta.persistence.JoinColumn;
  FROM: import javax.persistence.ManyToOne;
  TO:   import jakarta.persistence.ManyToOne;
  FROM: import javax.persistence.Table;
  TO:   import jakarta.persistence.Table;
  FROM: import javax.persistence.UniqueConstraint;
  TO:   import jakarta.persistence.UniqueConstraint;

InspectionReportEntity.java — imports:
  FROM: import javax.persistence.Column;
  TO:   import jakarta.persistence.Column;
  FROM: import javax.persistence.Entity;
  TO:   import jakarta.persistence.Entity;
  FROM: import javax.persistence.EnumType;
  TO:   import jakarta.persistence.EnumType;
  FROM: import javax.persistence.Enumerated;
  TO:   import jakarta.persistence.Enumerated;
  FROM: import javax.persistence.FetchType;
  TO:   import jakarta.persistence.FetchType;
  FROM: import javax.persistence.GeneratedValue;
  TO:   import jakarta.persistence.GeneratedValue;
  FROM: import javax.persistence.GenerationType;
  TO:   import jakarta.persistence.GenerationType;
  FROM: import javax.persistence.Id;
  TO:   import jakarta.persistence.Id;
  FROM: import javax.persistence.JoinColumn;
  TO:   import jakarta.persistence.JoinColumn;
  FROM: import javax.persistence.ManyToOne;
  TO:   import jakarta.persistence.ManyToOne;
  FROM: import javax.persistence.Table;
  TO:   import jakarta.persistence.Table;

SecurityConfigTest.java (test file) — 1 import:
  FROM: import javax.servlet.Filter;
  TO:   import jakarta.servlet.Filter;
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate javax.servlet.* to jakarta.servlet.* in main source files</name>
  <files>
    src/main/java/com/st4r4x/config/SecurityConfig.java,
    src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java,
    src/main/java/com/st4r4x/security/RateLimitFilter.java
  </files>
  <read_first>
    src/main/java/com/st4r4x/config/SecurityConfig.java,
    src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java,
    src/main/java/com/st4r4x/security/RateLimitFilter.java
  </read_first>
  <action>
In each file, replace ALL `import javax.servlet` lines with `import jakarta.servlet` as specified in the `<interfaces>` block above. This is purely a string substitution in the import section — do NOT change any other code.

Specific replacements per file:

SecurityConfig.java: One line to change:
  `import javax.servlet.http.HttpServletResponse;` → `import jakarta.servlet.http.HttpServletResponse;`

JwtAuthenticationFilter.java: Four lines to change:
  `import javax.servlet.FilterChain;` → `import jakarta.servlet.FilterChain;`
  `import javax.servlet.ServletException;` → `import jakarta.servlet.ServletException;`
  `import javax.servlet.http.HttpServletRequest;` → `import jakarta.servlet.http.HttpServletRequest;`
  `import javax.servlet.http.HttpServletResponse;` → `import jakarta.servlet.http.HttpServletResponse;`

RateLimitFilter.java: Four lines to change:
  `import javax.servlet.FilterChain;` → `import jakarta.servlet.FilterChain;`
  `import javax.servlet.ServletException;` → `import jakarta.servlet.ServletException;`
  `import javax.servlet.http.HttpServletRequest;` → `import jakarta.servlet.http.HttpServletRequest;`
  `import javax.servlet.http.HttpServletResponse;` → `import jakarta.servlet.http.HttpServletResponse;`

Do NOT change any other code in these files — method signatures, annotations, class declarations, and all logic stay exactly as-is.
  </action>
  <verify>
    <automated>grep -rn "import javax.servlet" src/main/java/com/st4r4x/config/SecurityConfig.java src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java src/main/java/com/st4r4x/security/RateLimitFilter.java 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import javax.servlet" src/main/java/com/st4r4x/config/SecurityConfig.java` returns NO output
    - `grep "import javax.servlet" src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java` returns NO output
    - `grep "import javax.servlet" src/main/java/com/st4r4x/security/RateLimitFilter.java` returns NO output
    - `grep "import jakarta.servlet.http.HttpServletResponse" src/main/java/com/st4r4x/config/SecurityConfig.java` returns a match
    - `grep "import jakarta.servlet.FilterChain" src/main/java/com/st4r4x/security/JwtAuthenticationFilter.java` returns a match
    - `grep "import jakarta.servlet.FilterChain" src/main/java/com/st4r4x/security/RateLimitFilter.java` returns a match
  </acceptance_criteria>
  <done>All javax.servlet imports in servlet filter files replaced with jakarta.servlet equivalents</done>
</task>

<task type="auto">
  <name>Task 2: Migrate javax.persistence.* to jakarta.persistence.* in entity files and fix SecurityConfigTest</name>
  <files>
    src/main/java/com/st4r4x/entity/UserEntity.java,
    src/main/java/com/st4r4x/entity/BookmarkEntity.java,
    src/main/java/com/st4r4x/entity/InspectionReportEntity.java,
    src/test/java/com/st4r4x/config/SecurityConfigTest.java
  </files>
  <read_first>
    src/main/java/com/st4r4x/entity/UserEntity.java,
    src/main/java/com/st4r4x/entity/BookmarkEntity.java,
    src/main/java/com/st4r4x/entity/InspectionReportEntity.java,
    src/test/java/com/st4r4x/config/SecurityConfigTest.java
  </read_first>
  <action>
In each entity file, replace ALL `import javax.persistence.*` lines with `import jakarta.persistence.*` as specified in the `<interfaces>` block. In SecurityConfigTest.java, replace the one servlet import.

UserEntity.java: Replace all 6 `import javax.persistence.*` lines with `import jakarta.persistence.*` equivalents. Exact replacements listed in `<interfaces>` above.

BookmarkEntity.java: Replace all 10 `import javax.persistence.*` lines with `import jakarta.persistence.*` equivalents. Exact replacements listed in `<interfaces>` above.

InspectionReportEntity.java: Replace all 11 `import javax.persistence.*` lines with `import jakarta.persistence.*` equivalents. Exact replacements listed in `<interfaces>` above.

SecurityConfigTest.java: One line to change:
  `import javax.servlet.Filter;` → `import jakarta.servlet.Filter;`

Do NOT change any other code. Annotation semantics are identical between javax.persistence and jakarta.persistence — `@Entity`, `@Table`, `@Column`, `@Id`, `@GeneratedValue`, `@ManyToOne`, `@JoinColumn`, `@Enumerated`, `@EnumType`, `@FetchType`, `@UniqueConstraint` all behave identically.

After completing all edits, run `mvn clean compile` and confirm it exits 0. If there are residual javax symbol errors, grep all source files for `import javax\.(servlet|persistence)` and fix any remaining occurrences.
  </action>
  <verify>
    <automated>grep -rn "import javax\.\(servlet\|persistence\)" src/main/java/ src/test/java/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "import javax.persistence" src/main/java/` returns NO output
    - `grep -rn "import javax.servlet" src/main/java/ src/test/java/` returns NO output
    - `grep "import jakarta.persistence.Entity" src/main/java/com/st4r4x/entity/UserEntity.java` returns a match
    - `grep "import jakarta.persistence.Entity" src/main/java/com/st4r4x/entity/BookmarkEntity.java` returns a match
    - `grep "import jakarta.persistence.Entity" src/main/java/com/st4r4x/entity/InspectionReportEntity.java` returns a match
    - `grep "import jakarta.servlet.Filter" src/test/java/com/st4r4x/config/SecurityConfigTest.java` returns a match
    - `mvn clean compile -q 2>&1 | tail -5` exits 0 (BUILD SUCCESS)
  </acceptance_criteria>
  <done>All javax.persistence.* and remaining javax.servlet.* imports replaced; mvn clean compile succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jakarta.persistence annotations → Hibernate 6 | JPA annotations interpreted by Hibernate 6 ORM; no external trust boundary |
| jakarta.servlet → Spring Security filter chain | Filter API called by Spring Security chain; input comes from HTTP layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-A | Tampering | JPA entity import rename | accept | Pure namespace rename; annotation semantics are identical in jakarta.persistence vs javax.persistence — no behavior change |
| T-21-02-B | Spoofing | SecurityConfigTest javax.servlet.Filter cast | accept | The `(Filter)` cast in SecurityConfigTest is test-only and casts a Spring Security bean; no security implication |
</threat_model>

<verification>
After both tasks:
1. `grep -rn "import javax.servlet\|import javax.persistence" src/` — must return 0 lines
2. `grep -rn "import jakarta.servlet\|import jakarta.persistence" src/main/java/` — must return multiple lines (one per import)
3. `mvn clean compile -q` — must exit 0

The command in step 3 is the blocking acceptance gate for this plan.
</verification>

<success_criteria>
- Zero `import javax.servlet` or `import javax.persistence` occurrences anywhere in src/
- `mvn clean compile` exits 0
- All 7 files have their jakarta.* imports in place
</success_criteria>

<output>
After completion, create `.planning/phases/21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju/21-02-SUMMARY.md`
</output>
