---
phase: 21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/resources/application.properties
  - src/test/resources/application-test.properties
autonomous: true
requirements: [UPGRADE-01]

must_haves:
  truths:
    - "pom.xml declares Spring Boot parent 4.0.5"
    - "pom.xml declares java.version 25 and maven.compiler source/target 25"
    - "pom.xml has springdoc-openapi-starter-webmvc-ui:2.8.6 (not springdoc-openapi-ui:1.8.0)"
    - "pom.xml has logstash-logback-encoder:8.1 (not 7.3)"
    - "pom.xml has no junit:junit:4.13.2 dependency"
    - "pom.xml has no junit-vintage-engine dependency"
    - "pom.xml JaCoCo exclusion patterns use com/st4r4x/ not com/aflokkat/"
    - "application.properties has no spring.mvc.pathmatch.matching-strategy line"
    - "application.properties has no spring.jpa.properties.hibernate.dialect line"
    - "application-test.properties has no spring.jpa.properties.hibernate.dialect line"
    - "mvn clean compile exits 0 (source tree unchanged — compile errors expected from javax, addressed in Plan 02)"
  artifacts:
    - path: "pom.xml"
      provides: "Boot 4.0.5 parent, Java 25 target, updated dependency coordinates, correct JaCoCo exclusions"
      contains: "spring-boot-starter-parent:4.0.5"
    - path: "src/main/resources/application.properties"
      provides: "Cleaned properties file without removed Boot 3+ properties"
    - path: "src/test/resources/application-test.properties"
      provides: "Cleaned test properties file without removed Boot 3+ properties"
  key_links:
    - from: "pom.xml spring-boot-starter-parent"
      to: "Spring Boot 4.0.5 BOM"
      via: "Maven parent resolution"
      pattern: "<version>4.0.5</version>"
    - from: "pom.xml springdoc artifact"
      to: "springdoc-openapi-starter-webmvc-ui:2.8.6"
      via: "Maven dependency"
      pattern: "springdoc-openapi-starter-webmvc-ui"
    - from: "pom.xml JaCoCo exclusions"
      to: "com/st4r4x/ class files"
      via: "JaCoCo exclude patterns"
      pattern: "com/st4r4x/dto/**"
---

<objective>
Upgrade pom.xml from Spring Boot 2.6.15 to 4.0.5 and Java 11 to 25. Update dependency coordinates for springdoc (v1 → v2 artifact rename), add logstash-logback-encoder 8.1, remove JUnit 4 explicit declarations and vintage engine. Fix JaCoCo exclusion patterns (wrong package com/aflokkat/ → correct com/st4r4x/). Clean application.properties and application-test.properties of removed Boot 3+ properties.

Purpose: This is the dependency foundation — every subsequent plan depends on the correct Boot version being declared here. The project will NOT compile after this plan (javax → jakarta errors surface), which is intentional and resolved in Plan 02.
Output: Updated pom.xml, application.properties, and application-test.properties. `mvn dependency:tree` resolves correctly. `mvn clean compile` is expected to FAIL with javax symbol errors (that is the correct intermediate state, not a problem).

## Boot 4.0.5 Compatibility Matrix (verified from in-session research)

| Dependency | Version | Boot 4 Compatible | Notes |
|------------|---------|-------------------|-------|
| Spring Boot | 4.0.5 | YES | Requires Java 17+; Java 25 explicitly encouraged |
| Spring Framework | 7.0 | YES | Bundled by Boot 4 |
| Spring Security | 7.0 | YES | requestMatchers API unchanged from Security 6; lambda DSL in Plan 03 correct |
| Bucket4j | 7.6.1 | YES | Java 11+ runtime requirement satisfied by Java 25 |
| jjwt | 0.11.5 | YES | No Jakarta EE dependency — pure java.crypto.* |
| Testcontainers | 1.20.1 | YES | Keep pinned; BOM may provide 2.x but 1.20.1 works fine with JUnit 5 |
| dotenv-java | 3.0.0 | YES | No servlet/JPA dependencies |
| MongoDB autoconfigure exclude class names | org.springframework.boot.autoconfigure.mongo.* | YES | VERIFIED: class names unchanged in Boot 4 — same package as Boot 2/3 |
| JaCoCo | 0.8.14 | YES | Explicit version pin; not managed by Boot BOM |
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pom.xml — Boot parent, Java version, dependency coordinates, JaCoCo exclusion fix</name>
  <files>pom.xml</files>
  <read_first>pom.xml</read_first>
  <action>
Apply ALL of the following changes to pom.xml in a single edit:

1. Spring Boot parent version: `<version>2.6.15</version>` → `<version>4.0.5</version>`

2. Java version property: `<java.version>11</java.version>` → `<java.version>25</java.version>`

3. Maven compiler source/target in `<properties>`:
   - `<maven.compiler.source>11</maven.compiler.source>` → `<maven.compiler.source>25</maven.compiler.source>`
   - `<maven.compiler.target>11</maven.compiler.target>` → `<maven.compiler.target>25</maven.compiler.target>`

4. Maven compiler plugin `<configuration>` block inside `maven-compiler-plugin`:
   - `<source>11</source>` → `<source>25</source>`
   - `<target>11</target>` → `<target>25</target>`

5. springdoc artifact: replace the entire `springdoc-openapi-ui:1.8.0` dependency block:
   ```xml
   <dependency>
     <groupId>org.springdoc</groupId>
     <artifactId>springdoc-openapi-ui</artifactId>
     <version>1.8.0</version>
   </dependency>
   ```
   with:
   ```xml
   <!-- SpringDoc OpenAPI v2 — required for Spring Boot 3+/4+ compatibility -->
   <dependency>
     <groupId>org.springdoc</groupId>
     <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
     <version>2.8.6</version>
   </dependency>
   ```

6. Add logstash-logback-encoder 8.1 in the logging section (after the spring-boot-starter-logging dependency):
   ```xml
   <!-- Structured JSON logging — 8.x targets Logback 1.5.x (Spring Boot 3+/4+ ships 1.5.x) -->
   <dependency>
     <groupId>net.logstash.logback</groupId>
     <artifactId>logstash-logback-encoder</artifactId>
     <version>8.1</version>
   </dependency>
   ```

7. Remove the explicit JUnit 4 dependency block entirely (these 5 lines):
   ```xml
   <dependency>
     <groupId>junit</groupId>
     <artifactId>junit</artifactId>
     <version>4.13.2</version>
     <scope>test</scope>
   </dependency>
   ```

8. Remove the junit-vintage-engine dependency block entirely:
   ```xml
   <!-- JUnit Vintage Engine: allows JUnit 4 @Test annotations to run on the JUnit 5 platform.
        Required because existing tests (AppConfigTest, AuthServiceTest, etc.) use JUnit 4. -->
   <dependency>
     <groupId>org.junit.vintage</groupId>
     <artifactId>junit-vintage-engine</artifactId>
     <scope>test</scope>
   </dependency>
   ```

9. Update the comment on the Testcontainers block — the "Still 1.x — JUnit 4 @ClassRule support preserved. Do NOT upgrade to 2.x." part of the comment should be updated to: "Still 1.x — Testcontainers 2.x changed the API; 1.20.1 is stable and compatible with JUnit 5 @BeforeAll static containers."

10. FIX JaCoCo exclusion patterns (BOTH jacoco-report and jacoco-check executions):
    The current exclusions use the wrong package path `com/aflokkat/` — the project compiles to `com/st4r4x/` (groupId is com.st4r4x).
    In BOTH jacoco-report and jacoco-check `<configuration>` blocks, replace:
    ```xml
    <excludes>
      <exclude>com/aflokkat/dto/**</exclude>
      <exclude>com/aflokkat/entity/**</exclude>
      <exclude>com/aflokkat/aggregation/**</exclude>
      <exclude>com/aflokkat/domain/**</exclude>
    </excludes>
    ```
    with:
    ```xml
    <!-- Exclude low-coverage POJOs/DTOs/entities from coverage threshold calculation -->
    <excludes>
      <exclude>com/st4r4x/dto/**</exclude>
      <exclude>com/st4r4x/entity/**</exclude>
      <exclude>com/st4r4x/aggregation/**</exclude>
      <exclude>com/st4r4x/domain/**</exclude>
    </excludes>
    ```
    This fix is required because the wrong package path caused all exclusions to match nothing,
    inflating the coverage denominator with low-coverage POJOs and potentially failing the 0.38 threshold.

Do NOT change: byte-buddy.version, argLine property, Surefire plugin config, Failsafe plugin config, JaCoCo threshold values, Testcontainers versions, Mockito versions, jjwt versions, Bucket4j version, any other dependency versions not listed above.
  </action>
  <verify>
    <automated>grep -n "4.0.5\|java.version.*25\|maven.compiler.source.*25\|springdoc-openapi-starter-webmvc-ui\|logstash-logback-encoder.*8.1\|com/st4r4x/dto" /home/missia03/Projects/restaurant-analytics/pom.xml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep "spring-boot-starter-parent" pom.xml` shows `<version>4.0.5</version>` on the adjacent line
    - `grep "java.version" pom.xml` shows `25` (not 11)
    - `grep "maven.compiler.source" pom.xml` shows `25` (not 11)
    - `grep "springdoc-openapi-starter-webmvc-ui" pom.xml` returns a match
    - `grep "springdoc-openapi-ui" pom.xml` returns NO match (old artifact removed)
    - `grep "logstash-logback-encoder" pom.xml` shows version `8.1`
    - `grep "junit:junit\|junit-vintage-engine" pom.xml` returns NO match
    - `grep "com/aflokkat" pom.xml` returns NO match (wrong package removed from JaCoCo exclusions)
    - `grep "com/st4r4x/dto" pom.xml` returns a match (correct package in JaCoCo exclusions)
    - `mvn dependency:tree -q 2>&1 | grep "BUILD SUCCESS"` exits 0
  </acceptance_criteria>
  <done>pom.xml declares Boot 4.0.5, Java 25, springdoc v2, logstash 8.1, no JUnit 4 artifacts, JaCoCo exclusions use correct com/st4r4x/ package</done>
</task>

<task type="auto">
  <name>Task 2: Clean application.properties and application-test.properties of removed Boot 3+ properties</name>
  <files>
    src/main/resources/application.properties,
    src/test/resources/application-test.properties
  </files>
  <read_first>
    src/main/resources/application.properties,
    src/test/resources/application-test.properties
  </read_first>
  <action>
**File 1: src/main/resources/application.properties**

Remove two property lines:

1. Remove this entire line (including the comment above it):
   ```
   # Swagger / OpenAPI (Spring Boot 2.6 compatibility)
   spring.mvc.pathmatch.matching-strategy=ant_path_matcher
   ```
   This property is REMOVED in Boot 3+. Boot 3 uses PathPatternParser exclusively; the old AntPathMatcher strategy is not configurable via this property. springdoc 2.x does not need it.

2. Remove this line from the JPA section:
   ```
   spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
   ```
   Hibernate 6 (shipped with Boot 3+) auto-detects the PostgreSQL dialect. Keeping the explicit value generates a deprecation warning and is unnecessary.

Keep all other properties exactly as-is. Keep the `springdoc.swagger-ui.path=/swagger-ui.html` and `springdoc.api-docs.path=/api-docs` lines — these are valid in springdoc 2.x.

Also keep the `# Swagger / OpenAPI` section comment if it still has other content below it (springdoc.* lines), but remove the comment line itself if it only referred to the ant_path_matcher property.

NOTE on MongoDB autoconfigure exclusion (keep as-is): The three class names in `spring.autoconfigure.exclude` are VERIFIED stable in Boot 4.0.5 — same `org.springframework.boot.autoconfigure.mongo.*` package as Boot 2/3. Do NOT change this line.

---

**File 2: src/test/resources/application-test.properties**

Remove this line from the JPA section:
```
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
```
Same reason as above: Hibernate 6 auto-detects the dialect.

Also update the comment above `spring.jpa.hibernate.ddl-auto=create`. The current comment references `@ClassRule` lifecycle (which will be removed from UserRepositoryIT in Plan 04). Replace it with:
```
# Let Hibernate create the schema in the TC container.
# Use "create" not "create-drop": containers are stopped in @AfterAll before Spring's
# SessionFactory shutdown hook fires for DDL drop. "create-drop" causes StackOverflowError
# in GenerationTargetToDatabase when the container is already stopped during schema drop.
# "create" skips the drop-on-close.
```

Keep all other properties in application-test.properties exactly as-is.
  </action>
  <verify>
    <automated>if grep -q "ant_path_matcher\|hibernate.dialect" /home/missia03/Projects/restaurant-analytics/src/main/resources/application.properties /home/missia03/Projects/restaurant-analytics/src/test/resources/application-test.properties; then echo "FAIL: removed properties still present"; exit 1; fi; echo "PASS: no removed properties found"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ant_path_matcher" src/main/resources/application.properties` returns NO output (exit code 1)
    - `grep "hibernate.dialect" src/main/resources/application.properties` returns NO output (exit code 1)
    - `grep "hibernate.dialect" src/test/resources/application-test.properties` returns NO output (exit code 1)
    - `grep "springdoc.swagger-ui.path" src/main/resources/application.properties` returns a match (line kept)
    - `grep "springdoc.api-docs.path" src/main/resources/application.properties` returns a match (line kept)
    - `grep "spring.jpa.hibernate.ddl-auto=update" src/main/resources/application.properties` returns a match (JPA DDL setting kept)
    - `grep "spring.jpa.hibernate.ddl-auto=create" src/test/resources/application-test.properties` returns a match (test DDL setting kept)
    - `grep "spring.autoconfigure.exclude" src/main/resources/application.properties` returns a match (MongoDB exclusions UNCHANGED — verified stable for Boot 4)
  </acceptance_criteria>
  <done>application.properties and application-test.properties have no removed Boot 3+ properties; springdoc 2.x properties intact; test properties comment updated for JUnit 5 lifecycle</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pom.xml → Maven Central | Dependency coordinates resolved from public registry; version pinning prevents supply-chain substitution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-A | Tampering | pom.xml dependency versions | mitigate | All versions explicitly pinned with comments; no `LATEST` or `RELEASE` references |
| T-21-01-B | Denial of Service | Boot 4.0.5 BOM pulling in incompatible transitive deps | accept | Risk accepted: Boot 4.0.5 BOM is stable; transitive resolution verified by `mvn dependency:tree` |
</threat_model>

<verification>
After both tasks complete:
1. `grep "4.0.5" pom.xml` — confirms Boot parent version
2. `grep "java.version" pom.xml` — confirms `25`
3. `grep "springdoc-openapi-starter-webmvc-ui" pom.xml` — confirms v2 artifact
4. `grep "logstash-logback-encoder" pom.xml` — confirms `8.1`
5. `grep "junit:junit\|junit-vintage" pom.xml` — returns nothing (JUnit 4 removed)
6. `grep "com/aflokkat" pom.xml` — returns nothing (JaCoCo exclusions fixed)
7. `grep "com/st4r4x/dto" pom.xml` — confirms correct JaCoCo exclusion path
8. `grep "ant_path_matcher\|hibernate.dialect" src/main/resources/application.properties` — returns nothing
9. `grep "hibernate.dialect" src/test/resources/application-test.properties` — returns nothing
10. `mvn dependency:tree -q` — resolves without error (confirms coordinates are correct)

NOTE: `mvn clean compile` is EXPECTED to fail after this plan with javax symbol errors. That is the correct intermediate state — Plan 02 addresses it.
</verification>

<success_criteria>
- pom.xml parent is Spring Boot 4.0.5
- java.version and maven.compiler.source/target are all 25
- springdoc-openapi-starter-webmvc-ui:2.8.6 in dependencies, old springdoc-openapi-ui:1.8.0 absent
- logstash-logback-encoder:8.1 in dependencies
- junit:junit:4.13.2 absent, junit-vintage-engine absent
- JaCoCo exclusions use com/st4r4x/ paths (not com/aflokkat/)
- ant_path_matcher and hibernate.dialect absent from application.properties
- hibernate.dialect absent from application-test.properties
- `mvn dependency:tree` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju/21-01-SUMMARY.md`
</output>
