---
phase: 21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju
plan: 04
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - src/test/java/com/st4r4x/aggregation/AggregationPojoTest.java
  - src/test/java/com/st4r4x/config/AppConfigTest.java
  - src/test/java/com/st4r4x/config/MongoClientFactoryTest.java
  - src/test/java/com/st4r4x/config/SecurityConfigTest.java
  - src/test/java/com/st4r4x/dao/RestaurantDAOImplTest.java
  - src/test/java/com/st4r4x/dao/RestaurantDAOIT.java
  - src/test/java/com/st4r4x/domain/RestaurantTest.java
  - src/test/java/com/st4r4x/util/ValidationUtilTest.java
  - src/test/java/com/st4r4x/repository/UserRepositoryIT.java
autonomous: true
requirements: [UPGRADE-04]

must_haves:
  truths:
    - "No import org.junit.Test, org.junit.Before, org.junit.After, org.junit.BeforeClass, org.junit.AfterClass, org.junit.Assert.*, org.junit.ClassRule, org.junit.runner.RunWith remain in any test file"
    - "No @RunWith annotation remains in any test file"
    - "All @Test(expected=X) patterns converted to @Test + assertThrows(X.class, () -> ...)"
    - "RestaurantDAOIT @ClassRule + @BeforeClass/@AfterClass migrated to @BeforeAll/@AfterAll static methods with @Container"
    - "UserRepositoryIT @RunWith(SpringRunner.class) replaced with @ExtendWith(SpringExtension.class)"
    - "UserRepositoryIT @ClassRule + @Before migrated to @BeforeEach + @BeforeAll/@AfterAll"
    - "mvn test-compile exits 0"
    - "mvn test exits 0 (Surefire passes all unit tests)"
  artifacts:
    - path: "src/test/java/com/st4r4x/dao/RestaurantDAOIT.java"
      provides: "JUnit 5 Testcontainers integration test with @BeforeAll/@AfterAll"
      contains: "import org.junit.jupiter.api.BeforeAll"
    - path: "src/test/java/com/st4r4x/repository/UserRepositoryIT.java"
      provides: "JUnit 5 Spring Boot integration test"
      contains: "import org.junit.jupiter.api.extension.ExtendWith"
  key_links:
    - from: "RestaurantDAOIT @ClassRule"
      to: "Testcontainers @BeforeAll container start"
      via: "JUnit 5 @BeforeAll static method"
      pattern: "@BeforeAll.*static"
    - from: "UserRepositoryIT @RunWith(SpringRunner.class)"
      to: "@ExtendWith(SpringExtension.class)"
      via: "Spring Test JUnit 5 integration"
      pattern: "@ExtendWith\\(SpringExtension.class\\)"
---

<objective>
Migrate all 9 test files from JUnit 4 to JUnit 5. This covers: import renames, annotation renames, Assert→Assertions migration, @Test(expected=X) → assertThrows conversion, @ClassRule/@BeforeClass/@AfterClass → @BeforeAll/@AfterAll (static) for Testcontainers tests, and @RunWith(SpringRunner.class) → @ExtendWith(SpringExtension.class) for Spring integration tests.

Purpose: JUnit 4 support (junit-vintage-engine) was removed in Plan 01. Tests will fail at runtime without this migration. Plan 03 fixed compilation — this plan fixes test execution.
Output: All 9 test files migrated to JUnit 5 API. `mvn test` passes all Surefire (unit) tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju/21-RESEARCH.md
</context>

<interfaces>
<!-- JUnit 4 → JUnit 5 import mapping (apply universally) -->

IMPORT REPLACEMENTS:
  import org.junit.Test;           → import org.junit.jupiter.api.Test;
  import org.junit.Before;         → import org.junit.jupiter.api.BeforeEach;
  import org.junit.After;          → import org.junit.jupiter.api.AfterEach;
  import org.junit.BeforeClass;    → import org.junit.jupiter.api.BeforeAll;
  import org.junit.AfterClass;     → import org.junit.jupiter.api.AfterAll;
  import org.junit.ClassRule;      → (remove — replaced by @BeforeAll container start)
  import org.junit.runner.RunWith; → (remove)
  import static org.junit.Assert.assertEquals;   → import static org.junit.jupiter.api.Assertions.assertEquals;
  import static org.junit.Assert.assertNotNull;  → import static org.junit.jupiter.api.Assertions.assertNotNull;
  import static org.junit.Assert.assertFalse;    → import static org.junit.jupiter.api.Assertions.assertFalse;
  import static org.junit.Assert.assertTrue;     → import static org.junit.jupiter.api.Assertions.assertTrue;
  import static org.junit.Assert.assertSame;     → import static org.junit.jupiter.api.Assertions.assertSame;
  import static org.junit.Assert.*;              → import static org.junit.jupiter.api.Assertions.*;
  Add when needed:
    import org.junit.jupiter.api.extension.ExtendWith;
    import org.junit.jupiter.api.BeforeEach;
    import org.junit.jupiter.api.AfterEach;
    import static org.junit.jupiter.api.Assertions.assertThrows;
    import org.springframework.test.context.junit.jupiter.SpringExtension;

ANNOTATION REPLACEMENTS:
  @Before   → @BeforeEach
  @After    → @AfterEach
  @BeforeClass (on static method) → @BeforeAll
  @AfterClass (on static method)  → @AfterAll
  @ClassRule → (remove from field; container lifecycle managed via @BeforeAll)
  @RunWith(SpringRunner.class) → @ExtendWith(SpringExtension.class)
  @RunWith(MockitoJUnitRunner.class) → @ExtendWith(MockitoExtension.class)

@Test(expected = X.class) CONVERSION:
  BEFORE:
    @Test(expected = SomeException.class)
    public void testFoo() {
        someCall();
    }

  AFTER:
    @Test
    void testFoo() {
        assertThrows(SomeException.class, () -> someCall());
    }

ASSERT METHOD MESSAGE ARGUMENT ORDER:
  JUnit 4: assertEquals("message", expected, actual)
  JUnit 5: assertEquals(expected, actual, "message")
  — NOTE: The message argument moves to LAST position in JUnit 5.
  — The existing tests in this project use assertEquals(expected, actual) WITHOUT a message
    argument in most cases, or use assertEquals("message", results.size(), ...) with the message first.
  — CAREFULLY check argument order when migrating assertEquals calls that have a String message argument.
  — Example that needs reordering:
    JUnit 4: assertEquals("Results should not be null", results, null) [unlikely]
    JUnit 5: assertEquals(null, results, "Results should not be null")
  — Simple two-arg assertEquals/assertTrue/assertFalse/assertNotNull with no message are safe to rename directly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 7 simple test files (no Spring context, no @ClassRule)</name>
  <files>
    src/test/java/com/st4r4x/aggregation/AggregationPojoTest.java,
    src/test/java/com/st4r4x/config/AppConfigTest.java,
    src/test/java/com/st4r4x/config/MongoClientFactoryTest.java,
    src/test/java/com/st4r4x/config/SecurityConfigTest.java,
    src/test/java/com/st4r4x/dao/RestaurantDAOImplTest.java,
    src/test/java/com/st4r4x/domain/RestaurantTest.java,
    src/test/java/com/st4r4x/util/ValidationUtilTest.java
  </files>
  <read_first>
    src/test/java/com/st4r4x/aggregation/AggregationPojoTest.java,
    src/test/java/com/st4r4x/config/AppConfigTest.java,
    src/test/java/com/st4r4x/config/MongoClientFactoryTest.java,
    src/test/java/com/st4r4x/config/SecurityConfigTest.java,
    src/test/java/com/st4r4x/dao/RestaurantDAOImplTest.java,
    src/test/java/com/st4r4x/domain/RestaurantTest.java,
    src/test/java/com/st4r4x/util/ValidationUtilTest.java
  </read_first>
  <action>
Read each file before editing. Apply the JUnit 4 → JUnit 5 migration as specified in the `<interfaces>` block. Details per file:

**AggregationPojoTest.java:**
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Replace all `import static org.junit.Assert.*` lines with `import static org.junit.jupiter.api.Assertions.*` equivalents
- No annotation changes needed (no @Before/@After)

**RestaurantDAOImplTest.java:**
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Replace `import static org.junit.Assert.assertTrue;` with `import static org.junit.jupiter.api.Assertions.assertTrue;`
- No annotation changes needed

**RestaurantTest.java:**
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Replace all `import static org.junit.Assert.*` lines with JUnit 5 equivalents
- No annotation changes needed

**MongoClientFactoryTest.java:**
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Replace `import static org.junit.Assert.assertNotNull;` and `import static org.junit.Assert.assertSame;` with JUnit 5 equivalents
- No annotation changes needed

**ValidationUtilTest.java:**
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Convert all `@Test(expected = X.class)` occurrences to `@Test` + `assertThrows` pattern.
  There are 10 such occurrences. Example conversion:
  ```java
  // BEFORE:
  @Test(expected = IllegalArgumentException.class)
  public void testRequireNonEmpty_NullString() {
      ValidationUtil.requireNonEmpty(null, "fieldName");
  }

  // AFTER:
  @Test
  void testRequireNonEmpty_NullString() {
      assertThrows(IllegalArgumentException.class, () ->
          ValidationUtil.requireNonEmpty(null, "fieldName"));
  }
  ```
  Add `import static org.junit.jupiter.api.Assertions.assertThrows;` at top.
  Apply this conversion to ALL 10 `@Test(expected=...)` occurrences in this file.

**AppConfigTest.java:**
- Replace `import org.junit.After;` with `import org.junit.jupiter.api.AfterEach;`
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Replace all `import static org.junit.Assert.*` lines with JUnit 5 equivalents
- Change `@After` annotation to `@AfterEach`
- Convert 2 `@Test(expected = IllegalStateException.class)` occurrences to `@Test` + `assertThrows`
  Example:
  ```java
  // BEFORE:
  @Test(expected = IllegalStateException.class)
  public void testGetJwtSecret_throwsWhenAbsent() throws Exception {
      clearDotenv();
      Properties props = getAppConfigProperties();
      savedJwtSecret = props.getProperty("jwt.secret");
      props.remove("jwt.secret");
      AppConfig.getJwtSecret();
  }

  // AFTER:
  @Test
  void testGetJwtSecret_throwsWhenAbsent() {
      assertThrows(IllegalStateException.class, () -> {
          clearDotenv();
          Properties props = getAppConfigProperties();
          savedJwtSecret = props.getProperty("jwt.secret");
          props.remove("jwt.secret");
          AppConfig.getJwtSecret();
      });
  }
  ```
  IMPORTANT: The lambda in assertThrows cannot throw checked exceptions directly. The `clearDotenv()` and `getAppConfigProperties()` methods throw `Exception`. Wrap the lambda body with a try-catch that rethrows as RuntimeException, OR change method signatures to throw RuntimeException. The simplest fix: change `throws Exception` on the private helper methods to instead catch exceptions and rethrow as `RuntimeException`, or use `assertThrows(IllegalStateException.class, () -> { ... })` where the lambda body re-throws checked exceptions as `RuntimeException`. The cleanest approach: make the private helpers (`clearDotenv`, `getAppConfigProperties`) throw `RuntimeException` instead of `Exception` — this allows direct use in lambdas.

**SecurityConfigTest.java:**
- Note: `import javax.servlet.Filter;` was already changed to `import jakarta.servlet.Filter;` in Plan 02.
- Replace `import org.junit.Before;` with `import org.junit.jupiter.api.BeforeEach;`
- Replace `import org.junit.After;` with `import org.junit.jupiter.api.AfterEach;`
- Replace `import org.junit.Test;` with `import org.junit.jupiter.api.Test;`
- Change `@Before` → `@BeforeEach`
- Change `@After` → `@AfterEach`
- No Assert imports to change (this file uses MockMvc status matchers, not JUnit Assert)

After all edits, run `mvn test-compile` and fix any compilation errors. Then run:
`mvn test -Dtest=AggregationPojoTest,MongoClientFactoryTest,ValidationUtilTest,AppConfigTest,SecurityConfigTest,RestaurantDAOImplTest,RestaurantTest`
to verify all 7 unit tests pass.
  </action>
  <verify>
    <automated>if grep -rn "import org.junit.Test;\|import org.junit.Before;\|import org.junit.After;\|import static org.junit.Assert\|@Test(expected" src/test/java/com/st4r4x/aggregation/ src/test/java/com/st4r4x/config/ src/test/java/com/st4r4x/dao/RestaurantDAOImplTest.java src/test/java/com/st4r4x/domain/ src/test/java/com/st4r4x/util/ 2>/dev/null; then echo "FAIL: JUnit 4 remnants found"; exit 1; fi; echo "PASS: no JUnit 4 remnants"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "import org.junit.Test\b" src/test/java/com/st4r4x/aggregation/ src/test/java/com/st4r4x/config/ src/test/java/com/st4r4x/dao/RestaurantDAOImplTest.java src/test/java/com/st4r4x/domain/ src/test/java/com/st4r4x/util/` returns NO output
    - `grep -rn "@Test(expected" src/test/java/com/st4r4x/util/ValidationUtilTest.java src/test/java/com/st4r4x/config/AppConfigTest.java` returns NO output
    - `grep -n "import org.junit.jupiter.api.Test" src/test/java/com/st4r4x/util/ValidationUtilTest.java` returns a match
    - `grep -n "assertThrows" src/test/java/com/st4r4x/util/ValidationUtilTest.java | wc -l` returns 10 or more
    - `grep -n "@BeforeEach\|@AfterEach" src/test/java/com/st4r4x/config/SecurityConfigTest.java | wc -l` returns 2
    - `mvn test-compile -q 2>&1 | tail -3` exits 0
    - `mvn test -Dtest=AggregationPojoTest,MongoClientFactoryTest,ValidationUtilTest,AppConfigTest,SecurityConfigTest,RestaurantDAOImplTest,RestaurantTest -q 2>&1 | tail -5` exits 0
  </acceptance_criteria>
  <done>7 simple test files fully migrated to JUnit 5; no @Test(expected) remains; mvn test passes for all 7 files</done>
</task>

<task type="auto">
  <name>Task 2: Migrate RestaurantDAOIT.java and UserRepositoryIT.java to JUnit 5 Testcontainers lifecycle</name>
  <files>
    src/test/java/com/st4r4x/dao/RestaurantDAOIT.java,
    src/test/java/com/st4r4x/repository/UserRepositoryIT.java
  </files>
  <read_first>
    src/test/java/com/st4r4x/dao/RestaurantDAOIT.java,
    src/test/java/com/st4r4x/repository/UserRepositoryIT.java
  </read_first>
  <action>
These two files use JUnit 4's `@ClassRule` pattern for Testcontainers. In JUnit 5, containers are started in `@BeforeAll` static methods or declared as `static` fields. Both approaches work with Testcontainers 1.20.1.

**RestaurantDAOIT.java — migration:**

Current JUnit 4 pattern:
```java
@ClassRule
public static MongoDBContainer mongoContainer = new MongoDBContainer("mongo:7.0");
```

Migrate to JUnit 5 static initialization (simplest — avoids @Testcontainers extension complications with @ClassRule):
1. Remove `import org.junit.ClassRule;`
2. Remove `import org.junit.BeforeClass;` → add `import org.junit.jupiter.api.BeforeAll;`
3. Remove `import org.junit.AfterClass;` → add `import org.junit.jupiter.api.AfterAll;`
4. Remove `import org.junit.Test;` → add `import org.junit.jupiter.api.Test;`
5. Replace `import static org.junit.Assert.*;` → `import static org.junit.jupiter.api.Assertions.*;`
6. Remove `@ClassRule` annotation from `mongoContainer` field declaration. Keep the field itself:
   ```java
   public static MongoDBContainer mongoContainer = new MongoDBContainer("mongo:7.0");
   ```
   Start and stop it explicitly in @BeforeAll / @AfterAll:
   ```java
   @BeforeAll
   public static void setUpClass() {
       mongoContainer.start();
       // ... rest of existing setUpClass body unchanged
   }

   @AfterAll
   public static void tearDownClass() {
       // ... existing body unchanged
       mongoContainer.stop();  // add this line at the end
   }
   ```
7. Change `@BeforeClass` → `@BeforeAll` annotation
8. Change `@AfterClass` → `@AfterAll` annotation
9. All `@Test` annotations stay (just import changes from org.junit to org.junit.jupiter.api)

IMPORTANT on assertions: JUnit 5 `assertEquals(expected, actual, message)` has message LAST. Check each assertion call — the existing JUnit 4 calls use `assertTrue("message", condition)` which in JUnit 5 becomes `assertTrue(condition, "message")`. The test file uses:
- `assertNotNull("Results should not be null", results)` → JUnit 5: `assertNotNull(results, "Results should not be null")`
- `assertFalse("Results should not be empty", results.isEmpty())` → JUnit 5: `assertFalse(results.isEmpty(), "Results should not be empty")`
- `assertTrue("Should have at least 5 boroughs", results.size() >= 5)` → JUnit 5: `assertTrue(results.size() >= 5, "Should have at least 5 boroughs")`
Apply this argument-order reordering to ALL assert calls that have a String message as the first argument.

**UserRepositoryIT.java — migration:**

This file uses `@RunWith(SpringRunner.class)` + `@SpringBootTest` + `@ClassRule` for containers + `@Before` for cleanup.

1. Remove `import org.junit.runner.RunWith;`
2. Remove `import org.junit.ClassRule;`
3. Remove `import org.junit.Before;` → add `import org.junit.jupiter.api.BeforeEach;`
4. Remove `import org.junit.AfterClass;` → add `import org.junit.jupiter.api.AfterAll;`
5. Remove `import org.junit.Test;` → add `import org.junit.jupiter.api.Test;`
6. Replace `import static org.junit.Assert.*;` → `import static org.junit.jupiter.api.Assertions.*;`
7. Add `import org.junit.jupiter.api.extension.ExtendWith;`
8. Add `import org.springframework.test.context.junit.jupiter.SpringExtension;`
9. Replace class annotation `@RunWith(SpringRunner.class)` → `@ExtendWith(SpringExtension.class)`
10. Remove `@ClassRule` from `pgContainer` and `mongoContainer` field declarations. Both containers are started in the existing `static {}` block (which already calls `.start()`). Add an `@AfterAll` static method to stop them:
    ```java
    @AfterAll
    public static void tearDownContainers() {
        System.clearProperty("mongodb.uri");
        if (pgContainer != null && pgContainer.isRunning()) pgContainer.stop();
        if (mongoContainer != null && mongoContainer.isRunning()) mongoContainer.stop();
    }
    ```
    Remove the old `@AfterClass tearDownClass()` method (which only did `System.clearProperty`).
11. Change `@Before` → `@BeforeEach` annotation on `cleanDatabase()`
12. Fix assertion argument order (String message moves to last):
    - `assertTrue("User should be found by username", found.isPresent())` → `assertTrue(found.isPresent(), "User should be found by username")`
    - `assertEquals("testuser@example.com", found.get().getEmail())` → no change (2-arg, no message)
    - `assertFalse("Should return empty Optional for unknown username", found.isPresent())` → `assertFalse(found.isPresent(), "Should return empty Optional for unknown username")`
    - `assertFalse("Bookmarks list should not be empty", bookmarks.isEmpty())` → `assertFalse(bookmarks.isEmpty(), "Bookmarks list should not be empty")`
    - `assertEquals("Should count 2 bookmarks for user", 2L, count)` → `assertEquals(2L, count, "Should count 2 bookmarks for user")`

Also update the `application-test.properties` comment that will now be stale: the comment above `spring.jpa.hibernate.ddl-auto=create` previously referenced `@ClassRule` lifecycle. It was already updated in Plan 01 Task 2 to reference `@AfterAll`. No change needed here — just verify the comment in application-test.properties is consistent with the @AfterAll pattern now in place.

After edits: run `mvn test-compile` to confirm 0 errors. The IT files are picked up by Failsafe (not Surefire) — they'll be fully verified in Plan 05.
  </action>
  <verify>
    <automated>if grep -rn "import org.junit.ClassRule\|import org.junit.runner.RunWith\|import org.junit.BeforeClass\|import org.junit.AfterClass\|@ClassRule\|@RunWith" src/test/java/com/st4r4x/dao/RestaurantDAOIT.java src/test/java/com/st4r4x/repository/UserRepositoryIT.java 2>/dev/null; then echo "FAIL: JUnit 4 ClassRule/RunWith remnants found"; exit 1; fi; echo "PASS: no JUnit 4 ClassRule/RunWith remnants"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@ClassRule\|@RunWith\|import org.junit.ClassRule\|import org.junit.runner" src/test/java/com/st4r4x/dao/RestaurantDAOIT.java src/test/java/com/st4r4x/repository/UserRepositoryIT.java` returns NO output
    - `grep "import org.junit.jupiter.api.BeforeAll" src/test/java/com/st4r4x/dao/RestaurantDAOIT.java` returns a match
    - `grep "@ExtendWith(SpringExtension.class)" src/test/java/com/st4r4x/repository/UserRepositoryIT.java` returns a match
    - `grep "@BeforeEach" src/test/java/com/st4r4x/repository/UserRepositoryIT.java` returns a match (cleanDatabase method)
    - `grep "import org.junit.jupiter.api.Test" src/test/java/com/st4r4x/dao/RestaurantDAOIT.java` returns a match
    - `mvn test-compile -q 2>&1 | tail -3` exits 0
    - `mvn test -q 2>&1 | tail -5` exits 0 (all Surefire unit tests pass — IT files excluded by Surefire by default)
  </acceptance_criteria>
  <done>RestaurantDAOIT and UserRepositoryIT migrated to JUnit 5; mvn test (Surefire) exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testcontainers container start → Docker socket | Container lifecycle calls Docker API; requires Docker running on executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-A | Tampering | JUnit 5 assertion argument order change | mitigate | Plan explicitly documents message-argument reordering for 3-arg assert calls; executor must verify each such call |
| T-21-04-B | Denial of Service | Testcontainers container lifecycle leak | mitigate | @AfterAll stops containers; existing static {} block starts them before Spring context |
| T-21-04-C | Information Disclosure | AppConfigTest reflection patches AppConfig static fields | accept | Test-only; reflection access is scoped to the test JVM process |
</threat_model>

<verification>
After Task 1 and Task 2:
1. `grep -rn "import org.junit.Test\b\|@ClassRule\|@RunWith\|import org.junit.Assert\|@Test(expected" src/test/java/` — must return 0 lines
2. `mvn test-compile -q` — must exit 0
3. `mvn test -q 2>&1 | tail -5` — must show BUILD SUCCESS (Surefire unit tests only; IT files excluded)

For IT files specifically, confirm:
- `grep "@BeforeAll" src/test/java/com/st4r4x/dao/RestaurantDAOIT.java` — match found
- `grep "@ExtendWith(SpringExtension.class)" src/test/java/com/st4r4x/repository/UserRepositoryIT.java` — match found
</verification>

<success_criteria>
- Zero `import org.junit.Test` (old package), `@ClassRule`, `@RunWith`, `@Test(expected=`, `import org.junit.Assert` occurrences in all test files
- All assertion calls with a String message argument have the message moved to last position (JUnit 5 signature)
- `mvn test-compile` exits 0
- `mvn test` exits 0 (Surefire unit tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/21-upgrade-java-11-25-and-spring-boot-2-6-15-4-0-5-including-ju/21-04-SUMMARY.md`
</output>
