---
phase: 14-testcontainers-integration-tests
plan: 04
type: execute
wave: 3
depends_on:
  - "14-02"
  - "14-03"
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - TEST-04
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "mvn verify exits 0 — full suite (unit tests + both IT tests) passes"
    - "mvn test exits 0 — no regressions to existing 28+ unit tests under Surefire"
    - "mvn failsafe:integration-test exits 0 — both RestaurantDAOIT and UserRepositoryIT run and pass"
    - "CHANGELOG.md has a Phase 14 entry documenting the Testcontainers migration"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Phase 14 changelog entry"
      contains: "Phase 14"
  key_links:
    - from: "mvn verify"
      to: "Failsafe integration-test goal"
      via: "maven-failsafe-plugin bound to verify lifecycle"
      pattern: "integration-test.*verify"
---

<objective>
Run the full Maven build to confirm all tests pass, then update CHANGELOG.md with the Phase 14 entry.

Purpose: This is the phase gate verification. Plans 01-03 deliver the individual pieces; this plan confirms the complete picture: `mvn verify` exits 0 with no regressions, and the CHANGELOG documents what shipped.

Output: CHANGELOG.md updated, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-testcontainers-integration-tests/14-02-SUMMARY.md
@.planning/phases/14-testcontainers-integration-tests/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite and update CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (read current content — append Phase 14 entry under [Unreleased])
    - .planning/phases/14-testcontainers-integration-tests/14-02-SUMMARY.md (confirm RestaurantDAOIT result)
    - .planning/phases/14-testcontainers-integration-tests/14-03-SUMMARY.md (confirm UserRepositoryIT result)
  </read_first>
  <action>
Step 1 — Run the full Maven build to confirm the phase gate passes:

```bash
cd /home/missia03/Projects/restaurant-analytics
mvn verify 2>&1
```

Expected output contains:
- `BUILD SUCCESS`
- `Tests run: N, Failures: 0, Errors: 0` for Surefire (unit tests)
- `Tests run: 14, Failures: 0, Errors: 0` for RestaurantDAOIT (Failsafe)
- `Tests run: 4, Failures: 0, Errors: 0` for UserRepositoryIT (Failsafe)

If `mvn verify` fails:
- If `RestaurantDAOIT` fails: check that `RestaurantDAOIntegrationTest.java` is deleted and the `MONGODB_URI` system property conflict is resolved.
- If `UserRepositoryIT` fails with `PSQLException`: check that the static block starts containers before `Initializer.initialize()` is called.
- If `UserRepositoryIT` fails with `RedisConnectionException`: confirm `@MockBean RestaurantCacheService` is present.
- Fix the failing test before proceeding to Step 2.

Step 2 — Add a Phase 14 entry to CHANGELOG.md under the `## [Unreleased]` section. Follow the existing format in CHANGELOG.md. Add:

```
### Phase 14: Testcontainers Integration Tests (2026-04-12)
- Migrated RestaurantDAOIntegrationTest to RestaurantDAOIT using Testcontainers mongo:7.0 — no live MongoDB required
- Added UserRepositoryIT covering UserRepository.save/findByUsername and BookmarkRepository.save/findByUserId against Testcontainers postgres:15-alpine
- Added maven-failsafe-plugin bound to integration-test and verify goals; IT tests use *IT.java naming convention
- Added Testcontainers 1.19.8 (testcontainers, mongodb, postgresql) as test-scope dependencies
- Fixed Surefire argLine to use @{argLine} late-binding to support future JaCoCo integration
- Added System.getProperty(key) tier-0 lookup to AppConfig.getProperty() for TC URI injection
```

The date `2026-04-12` reflects today's date (from `currentDate` in system context).
  </action>
  <verify>
    <automated>cd /home/missia03/Projects/restaurant-analytics && mvn verify 2>&amp;1 | grep -E "BUILD|Tests run|Failures|Errors" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mvn verify` exits 0 (exit code 0)
    - `mvn verify` output contains "BUILD SUCCESS"
    - `mvn verify` output shows Surefire "Tests run" with "Failures: 0, Errors: 0"
    - `mvn verify` output shows Failsafe "Tests run: 14" (RestaurantDAOIT) with "Failures: 0, Errors: 0"
    - `mvn verify` output shows Failsafe "Tests run: 4" (UserRepositoryIT) with "Failures: 0, Errors: 0"
    - `grep "Phase 14" CHANGELOG.md` returns a match
    - `grep "Testcontainers" CHANGELOG.md` returns a match
    - `grep "RestaurantDAOIT" CHANGELOG.md` returns a match
  </acceptance_criteria>
  <done>mvn verify exits 0 with all IT tests passing. CHANGELOG.md updated with Phase 14 entry.</done>
</task>

</tasks>

<verification>
Phase gate check:
1. `mvn verify` exits 0
2. `mvn test -q` exits 0 (unit tests only, fast path)
3. `grep "Phase 14" CHANGELOG.md` returns a match
4. `ls src/test/java/com/aflokkat/dao/` — only `RestaurantDAOIT.java` (no old IntegrationTest)
5. `ls src/test/java/com/aflokkat/repository/` — contains `UserRepositoryIT.java`
</verification>

<success_criteria>
- mvn verify exits 0 (full suite green)
- Surefire: all existing unit tests pass (no regressions)
- Failsafe: RestaurantDAOIT (14 tests) + UserRepositoryIT (4 tests) all pass
- CHANGELOG.md updated with Phase 14 entry
- No live MongoDB or PostgreSQL required to run the integration suite
</success_criteria>

<output>
After completion, create `.planning/phases/14-testcontainers-integration-tests/14-04-SUMMARY.md`
</output>
