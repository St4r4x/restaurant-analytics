---
phase: 14-testcontainers-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/java/com/aflokkat/config/AppConfig.java
autonomous: true
requirements:
  - TEST-04
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "mvn test exits 0 with no regressions after pom.xml changes"
    - "AppConfig.getProperty() returns a System.getProperty() value when set, bypassing System.getenv()"
    - "pom.xml contains Failsafe plugin bound to integration-test and verify goals"
    - "pom.xml contains testcontainers 1.19.8, mongodb 1.19.8, and postgresql 1.19.8 as test-scoped deps"
  artifacts:
    - path: "pom.xml"
      provides: "Failsafe plugin + Testcontainers 1.19.8 dependencies + fixed Surefire argLine"
      contains: "maven-failsafe-plugin"
    - path: "src/main/java/com/aflokkat/config/AppConfig.java"
      provides: "System.getProperty() tier-0 lookup before System.getenv()"
      contains: "System.getProperty(key)"
  key_links:
    - from: "RestaurantDAOIT.java (Wave 2)"
      to: "AppConfig.getProperty()"
      via: "System.setProperty(\"mongodb.uri\", ...)"
      pattern: "System\\.getProperty\\(key\\)"
---

<objective>
Add Testcontainers 1.19.8 dependencies to pom.xml, wire the Maven Failsafe plugin, fix the literal Surefire argLine, and add a System.getProperty() tier-0 lookup to AppConfig — the injection point that allows Testcontainers tests to override MongoDB and datasource URIs before MongoClientFactory initializes.

Purpose: This is the infrastructure foundation. Without it, the *IT.java test files produced in Wave 2 cannot compile (missing TC classes), cannot run (Failsafe absent), and cannot inject URIs (AppConfig never checks System.getProperty()).

Output: A pom.xml with Failsafe + TC 1.19.8 and an AppConfig that checks System.getProperty(key) before any env lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-testcontainers-integration-tests/14-CONTEXT.md
@.planning/phases/14-testcontainers-integration-tests/14-RESEARCH.md

<interfaces>
<!-- AppConfig.getProperty() current implementation — must be modified -->
<!-- From src/main/java/com/aflokkat/config/AppConfig.java lines 126-140 -->
```java
private static String getProperty(String key, String defaultValue) {
    // 1. System environment variable (Docker, CI...)
    String envKey = key.replace(".", "_").toUpperCase();
    String envValue = System.getenv(envKey);
    if (envValue != null) return envValue;

    // 2. Fichier .env
    if (dotenv != null) {
        String dotenvValue = dotenv.get(envKey, null);
        if (dotenvValue != null) return dotenvValue;
    }

    // 3. application.properties
    return properties.getProperty(key, defaultValue);
}
```

<!-- MongoClientFactory.getInstance() — reads AppConfig.getMongoUri() lazily -->
<!-- From src/main/java/com/aflokkat/config/MongoClientFactory.java -->
```java
public static synchronized MongoClient getInstance() {
    if (mongoClient == null) {
        mongoClient = MongoClients.create(AppConfig.getMongoUri());
    }
    return mongoClient;
}

public static synchronized void closeInstance() {
    if (mongoClient != null) {
        mongoClient.close();
        mongoClient = null;
    }
}
```

<!-- Current pom.xml Surefire configuration — argLine is a literal string (must be fixed) -->
```xml
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-failsafe-plugin</artifactId>  <!-- DOES NOT EXIST YET -->
  ...
</plugin>

<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-surefire-plugin</artifactId>
  <configuration>
    <argLine>-XX:+EnableDynamicAgentLoading</argLine>   <!-- literal — must become @{argLine} -->
  </configuration>
</plugin>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Testcontainers dependencies + Failsafe plugin to pom.xml</name>
  <files>pom.xml</files>
  <read_first>
    - pom.xml (read current state — current branch is pre-Phase-12, no Failsafe, no TC, literal Surefire argLine)
  </read_first>
  <behavior>
    - After change: `mvn test` still exits 0 (no Surefire regressions)
    - After change: `mvn failsafe:integration-test` runs without "No plugin found" error
    - After change: TC classes are on the test classpath (compile succeeds with TC imports)
  </behavior>
  <action>
Make the following three changes to pom.xml:

1. Add three Testcontainers dependencies inside the `<dependencies>` block (after the existing junit 4.13.2 entry). All must be `<scope>test</scope>` and pinned at `1.19.8`. Do NOT use 2.x — this project uses JUnit Vintage (JUnit 4) and TC 2.x dropped JUnit 4 support:

```xml
<!-- Testcontainers — pinned at 1.19.8, do NOT use 2.x (dropped JUnit 4 support) -->
<dependency>
  <groupId>org.testcontainers</groupId>
  <artifactId>testcontainers</artifactId>
  <version>1.19.8</version>
  <scope>test</scope>
</dependency>
<dependency>
  <groupId>org.testcontainers</groupId>
  <artifactId>mongodb</artifactId>
  <version>1.19.8</version>
  <scope>test</scope>
</dependency>
<dependency>
  <groupId>org.testcontainers</groupId>
  <artifactId>postgresql</artifactId>
  <version>1.19.8</version>
  <scope>test</scope>
</dependency>
```

2. Fix the Surefire argLine from the literal `-XX:+EnableDynamicAgentLoading` to the late-binding form `@{argLine} -XX:+EnableDynamicAgentLoading`. This allows JaCoCo (Phase 12) to prepend its agent without being silently overwritten. Find the existing Surefire `<argLine>` element and update it:

```xml
<argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
```

3. Add the maven-failsafe-plugin inside the `<plugins>` block (after the Surefire plugin). Bind it to the `integration-test` and `verify` goals with the same late-binding argLine pattern. The `<includes>` pattern `**/*IT.java` is Failsafe's default but must be confirmed via explicit config to ensure no drift:

```xml
<!-- Failsafe: runs *IT.java in integration-test phase, separate from Surefire unit tests -->
<plugin>
  <groupId>org.apache.maven.plugins</groupId>
  <artifactId>maven-failsafe-plugin</artifactId>
  <configuration>
    <argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
    <includes>
      <include>**/*IT.java</include>
    </includes>
  </configuration>
  <executions>
    <execution>
      <id>integration-tests</id>
      <goals>
        <goal>integration-test</goal>
        <goal>verify</goal>
      </goals>
    </execution>
  </executions>
</plugin>
```
  </action>
  <verify>
    <automated>cd /home/missia03/Projects/restaurant-analytics && mvn test -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pom.xml` contains `<artifactId>testcontainers</artifactId>` with `<version>1.19.8</version>`
    - `pom.xml` contains `<artifactId>mongodb</artifactId>` with `<version>1.19.8</version>` and `<scope>test</scope>`
    - `pom.xml` contains `<artifactId>postgresql</artifactId>` with `<version>1.19.8</version>` and `<scope>test</scope>`
    - `pom.xml` contains `<artifactId>maven-failsafe-plugin</artifactId>`
    - `pom.xml` contains `<goal>integration-test</goal>` inside Failsafe executions
    - `pom.xml` Surefire `<argLine>` value is `@{argLine} -XX:+EnableDynamicAgentLoading` (not the literal form)
    - `mvn test -q` exits 0 (all existing unit tests pass)
  </acceptance_criteria>
  <done>pom.xml has TC 1.19.8 (3 artifacts), Failsafe plugin bound to integration-test+verify, Surefire argLine uses @{argLine} late-binding, and mvn test exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add System.getProperty() tier-0 to AppConfig.getProperty()</name>
  <files>src/main/java/com/aflokkat/config/AppConfig.java</files>
  <read_first>
    - src/main/java/com/aflokkat/config/AppConfig.java (read entire file — the private static getProperty() method at lines 126-140 is the target)
    - src/main/java/com/aflokkat/config/MongoClientFactory.java (understand what calls AppConfig.getMongoUri() to confirm injection chain)
  </read_first>
  <behavior>
    - After change: `System.setProperty("mongodb.uri", "mongodb://localhost:12345")` → `AppConfig.getMongoUri()` returns `"mongodb://localhost:12345"`
    - After change: If `System.getProperty("mongodb.uri")` is null, fallback to System.getenv() → dotenv → properties (unchanged behavior for production)
    - After change: `mvn test` exits 0 — no regression in AppConfigTest (which patches AppConfig.properties via reflection, not via System.getProperty)
  </behavior>
  <action>
Modify the private static `getProperty(String key, String defaultValue)` method in `AppConfig.java` to add a tier-0 `System.getProperty(key)` check BEFORE the existing `System.getenv()` check.

The key insight: `System.setProperty("mongodb.uri", mongoContainer.getConnectionString())` uses the dotted properties-style key `"mongodb.uri"`. The existing tier-1 check uses `key.replace(".", "_").toUpperCase()` which would produce `"MONGODB_URI"` — a different key. The new tier-0 check uses the raw `key` as-is (`"mongodb.uri"`), matching the injection call.

Replace the existing method body with:

```java
private static String getProperty(String key, String defaultValue) {
    // 0. JVM system property — used by Testcontainers tests to inject container URIs
    //    before MongoClientFactory static singleton initializes.
    //    System.setProperty("mongodb.uri", ...) is the correct injection call.
    String sysProp = System.getProperty(key);
    if (sysProp != null) return sysProp;

    // 1. System environment variable (Docker, CI...)
    String envKey = key.replace(".", "_").toUpperCase();
    String envValue = System.getenv(envKey);
    if (envValue != null) return envValue;

    // 2. Fichier .env
    if (dotenv != null) {
        String dotenvValue = dotenv.get(envKey, null);
        if (dotenvValue != null) return dotenvValue;
    }

    // 3. application.properties
    return properties.getProperty(key, defaultValue);
}
```

Do NOT modify any other method in AppConfig.java. The public API (`getMongoUri()`, `getJwtSecret()`, etc.) is unchanged.
  </action>
  <verify>
    <automated>cd /home/missia03/Projects/restaurant-analytics && mvn test -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `AppConfig.java` line matching `System.getProperty(key)` exists before the `System.getenv(envKey)` line
    - `AppConfig.java` contains a comment mentioning "Testcontainers" near the new tier-0 block
    - `mvn test -q` exits 0 (AppConfigTest and all existing tests pass — AppConfigTest patches `AppConfig.properties` via reflection which is still tier-3, so it is unaffected)
    - `grep -n "System.getProperty" src/main/java/com/aflokkat/config/AppConfig.java` returns exactly one match in `getProperty()` method body
  </acceptance_criteria>
  <done>AppConfig.getProperty() checks System.getProperty(key) as tier-0 before any env lookup. Production behavior unchanged: no JVM property is set in production, so tier-1 (System.getenv) still applies first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JVM process boundary | System.setProperty() can only be called from within the JVM — no external input path |
| Test scope vs production scope | AppConfig tier-0 only matters when a caller has set a JVM property; production deployments never set mongodb.uri via System.setProperty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | AppConfig.getProperty() tier-0 | accept | System.setProperty() is only callable from within the same JVM process — no external caller can invoke it. In production (Docker/CI), no code sets System.getProperty("mongodb.uri") so the tier-0 path is never taken. Tier-1 (System.getenv) continues to be the effective first lookup in production. |
| T-14-02 | Information Disclosure | Testcontainers container URIs in JVM properties | accept | TC-assigned URIs (random localhost ports) are visible only within the test JVM process lifetime. No secrets are stored. Container is torn down after tests. System.clearProperty() called in @AfterClass removes the property. |
| T-14-03 | Elevation of Privilege | maven-failsafe-plugin running IT tests | accept | IT tests run in the same user context as the developer. No elevated privileges required. TC requires Docker daemon access which is already present (Docker 29.3.1 verified). |

**Phase 14 security summary:** This phase adds test infrastructure only. No new network endpoints, no authentication changes, no production code paths beyond the AppConfig tier-0 addition. The tier-0 addition does not weaken production security: JVM system properties cannot be set by external callers. ASVS Level 1 controls are not applicable to test infrastructure. Threat level: LOW.
</threat_model>

<verification>
After both tasks complete:
1. `mvn test -q` exits 0 — all existing unit tests pass (no Surefire regressions)
2. `grep "maven-failsafe-plugin" pom.xml` returns a match
3. `grep "testcontainers" pom.xml | grep "1.19.8"` returns 3 matches (core, mongodb, postgresql)
4. `grep "System.getProperty(key)" src/main/java/com/aflokkat/config/AppConfig.java` returns 1 match
5. `grep "@{argLine}" pom.xml` returns 2 matches (Surefire and Failsafe)
</verification>

<success_criteria>
- pom.xml contains 3 Testcontainers test-scope dependencies at version 1.19.8
- pom.xml has maven-failsafe-plugin bound to integration-test and verify goals
- pom.xml Surefire argLine is @{argLine} -XX:+EnableDynamicAgentLoading
- AppConfig.getProperty() returns System.getProperty(key) when set, before any env lookup
- mvn test exits 0 with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-testcontainers-integration-tests/14-01-SUMMARY.md`
</output>
