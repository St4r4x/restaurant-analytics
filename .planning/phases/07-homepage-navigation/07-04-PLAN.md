---
phase: 07-homepage-navigation
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/templates/profile.html
autonomous: true
gap_closure: true
requirements: [NAV-04]

must_haves:
  truths:
    - "Profile page shows up to 3 bookmark mini-cards (name, borough, grade badge, View Details link)"
    - "When the user has no bookmarks, a friendly empty-state message is shown instead of cards"
    - "Report count stat is still visible for CONTROLLER users"
  artifacts:
    - path: src/main/resources/templates/profile.html
      provides: "Bookmark list section replacing the numeric bookmarkCount stat"
      contains: "/api/users/me/bookmarks"
  key_links:
    - from: src/main/resources/templates/profile.html
      to: /api/users/me/bookmarks
      via: fetchWithAuth in DOMContentLoaded
      pattern: "fetchWithAuth.*me/bookmarks"
---

<objective>
Replace the `bookmarkCount` number stat on the profile page with a mini-card list of up to 3
bookmarks, consistent with the bookmarks strip on index.html.

Purpose: UAT test 5 reported "a list of bookmarks would be better than a number". The endpoint
`GET /api/users/me/bookmarks` already exists and returns the same shape consumed by `index.html`.

Output: Updated `profile.html` — no backend changes.
</objective>

<execution_context>
@/home/missia03/.claude/get-shit-done/workflows/execute-plan.md
@/home/missia03/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bookmarkCount stat with bookmark mini-cards in profile.html</name>
  <files>src/main/resources/templates/profile.html</files>
  <action>
    In `profile.html`, make the following targeted changes — do NOT touch layout, styles, or
    the `/api/users/me` fetch that populates username / email / role / reportCount.

    1. Remove the `bookmarkCount` stat block from the stats row built inside the `/api/users/me`
       `.then()` handler. Specifically, remove:
         ```
         var statsHtml = '<div style="text-align:center">'
           + '<div style="font-weight:700;font-size:1em;color:#333">' + (d.bookmarkCount != null ? d.bookmarkCount : '—') + '</div>'
           + '<div style="font-size:0.82em;color:#555;margin-top:4px">Bookmarks</div>'
           + '</div>';
         ```
       Keep the `reportCount` block and `document.getElementById('p-stats').innerHTML = statsHtml`
       assignment unchanged.

    2. Add a `#bookmarks-section` div below `#profile-card` (but still inside `.container`):
         ```html
         <div id="bookmarks-section" class="card" style="max-width:480px;margin:16px auto">
           <h3 style="font-size:1.1em;font-weight:700;margin-bottom:16px;border-bottom:3px solid #667eea;padding-bottom:8px">
             Your Bookmarks
           </h3>
           <div id="bookmarks-grid" style="display:grid;grid-template-columns:repeat(3,1fr);gap:16px">
             <div style="background:#f0f2ff;border-radius:8px;height:80px"></div>
             <div style="background:#f0f2ff;border-radius:8px;height:80px"></div>
             <div style="background:#f0f2ff;border-radius:8px;height:80px"></div>
           </div>
         </div>
         ```

    3. Add `.mini-card` to the existing `&lt;style&gt;` block (same rule as index.html):
         ```css
         .mini-card { background:white;border:1px solid #e8e8e8;border-radius:8px;padding:16px; }
         ```

    4. Copy `gradeBadgeHtml` and `renderRestaurantCards` verbatim from `index.html` into
       `profile.html`'s `&lt;script&gt;` block, placed before the `/api/users/me` fetch.

    5. After the existing `/api/users/me` fetch block, add a second fetch for bookmarks:
         ```javascript
         fetchWithAuth('/api/users/me/bookmarks')
           .then(r => r.json())
           .then(res => {
             renderRestaurantCards('bookmarks-grid', res.data,
               '<p style="color:#666;font-size:0.88em">You haven\'t bookmarked any restaurants yet.</p>');
           })
           .catch(() => {
             document.getElementById('bookmarks-grid').innerHTML =
               '<p style="color:#b71c1c;font-size:0.85em">Could not load bookmarks.</p>';
           });
         ```

    Do NOT change: the redirect-if-no-token guard, `fetchWithAuth` definition, the `/api/users/me`
    fetch, role badge logic, reportCount logic, or any CSS already present.
  </action>
  <verify>
    <automated>mvn clean package -DskipTests -q &amp;&amp; echo "BUILD OK"</automated>
  </verify>
  <done>
    - Profile page renders a "Your Bookmarks" section with up to 3 mini-cards (name, borough,
      grade badge, "View Details" link) loaded from `/api/users/me/bookmarks`
    - Empty state message shown when user has no bookmarks
    - `bookmarkCount` number no longer appears; `reportCount` stat still appears for controllers
    - Build succeeds with no compilation errors
  </done>
</task>

</tasks>

<verification>
After task completion:
1. Build succeeds: `mvn clean package -DskipTests`
2. Log in as `customer_test` and visit `/profile` — "Your Bookmarks" section visible with cards
   or empty-state message; no numeric bookmark count
3. Log in as `controller_test` and visit `/profile` — report count stat still shown; bookmarks
   section shown (likely empty)
</verification>

<success_criteria>
- `profile.html` calls `GET /api/users/me/bookmarks` and renders up to 3 mini-cards
- `gradeBadgeHtml` / `renderRestaurantCards` pattern reused verbatim from `index.html`
- `bookmarkCount` number removed; `reportCount` preserved
- Maven build green
</success_criteria>

<output>
After completion, create `.planning/phases/07-homepage-navigation/07-04-SUMMARY.md`
</output>
