---
phase: 04-integration-polish
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/main/java/com/aflokkat/controller/ViewController.java
  - src/main/java/com/aflokkat/controller/RestaurantController.java
  - src/main/resources/templates/hygiene-radar.html
  - src/main/resources/templates/inspection.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "ViewController.java no longer contains /hygiene-radar or /inspection routes"
    - "RestaurantController.java no longer contains /worst-cuisines, /cuisine-scores, or /popular-cuisines endpoints"
    - "hygiene-radar.html and inspection.html files do not exist in templates/"
    - "mvn test passes with no failures after removal"
  artifacts:
    - path: "src/main/java/com/aflokkat/controller/ViewController.java"
      provides: "View router — only live page routes"
    - path: "src/main/java/com/aflokkat/controller/RestaurantController.java"
      provides: "REST controller — only actively-used endpoints"
  key_links:
    - from: "ViewController.java"
      to: "live templates"
      via: "@GetMapping"
      pattern: "return \"index\"|return \"login\"|return \"restaurant\"|return \"inspection-map\"|return \"my-bookmarks\""
---

<objective>
Remove orphaned routes from ViewController.java (/hygiene-radar, /inspection), remove
dead REST endpoints from RestaurantController.java (/worst-cuisines, /cuisine-scores,
/popular-cuisines), and delete the orphaned templates hygiene-radar.html and inspection.html.

Purpose: The hygiene-radar and inspection pages are not linked from any current template.
The three RestaurantController endpoints (/worst-cuisines, /cuisine-scores, /popular-cuisines)
were used only by hygiene-radar.html, which is now removed. Deleting dead code reduces surface
area before tests are written.
Output: ViewController with 5 routes (not 7), RestaurantController without 3 dead endpoints,
no orphaned template files.
</objective>

<execution_context>
@/home/missia03/.claude/get-shit-done/workflows/execute-plan.md
@/home/missia03/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-polish/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned routes from ViewController and delete orphaned templates</name>
  <files>
    src/main/java/com/aflokkat/controller/ViewController.java,
    src/main/resources/templates/hygiene-radar.html,
    src/main/resources/templates/inspection.html
  </files>
  <read_first>
    - src/main/java/com/aflokkat/controller/ViewController.java (verify current routes before editing)
    - src/main/resources/templates/hygiene-radar.html (confirm it is orphaned before deleting)
    - src/main/resources/templates/inspection.html (confirm it is orphaned before deleting)
  </read_first>
  <action>
    **ViewController.java — remove two orphaned route methods:**

    Remove the entire `hygieneRadar()` method:
    ```java
    @GetMapping("/hygiene-radar")
    public String hygieneRadar() {
        return "hygiene-radar";
    }
    ```

    Remove the entire `inspectionDashboard()` method:
    ```java
    @GetMapping("/inspection")
    public String inspectionDashboard() {
        return "inspection";
    }
    ```

    After removal, ViewController.java must contain exactly these 5 routes:
    - `@GetMapping("/")` → `return "index"`
    - `@GetMapping("/login")` → `return "login"`
    - `@GetMapping("/restaurant/{id}")` → `return "restaurant"`
    - `@GetMapping("/inspection-map")` → `return "inspection-map"`
    - `@GetMapping("/my-bookmarks")` → `return "my-bookmarks"`

    Do NOT change the class Javadoc, package declaration, imports, or any other method.

    **Delete orphaned templates:**
    - Delete `src/main/resources/templates/hygiene-radar.html`
    - Delete `src/main/resources/templates/inspection.html`

    Use the Bash tool to delete these files:
    ```
    trash src/main/resources/templates/hygiene-radar.html
    trash src/main/resources/templates/inspection.html
    ```
    (Use `trash` as per project convention, not `rm`)
  </action>
  <verify>
    <automated>grep -n "hygiene-radar\|inspectionDashboard\|/inspection\"" src/main/java/com/aflokkat/controller/ViewController.java && echo "FAIL: orphaned routes still present" || echo "PASS: routes removed"; ls src/main/resources/templates/hygiene-radar.html 2>/dev/null && echo "FAIL: hygiene-radar.html still exists" || echo "PASS: deleted"; ls src/main/resources/templates/inspection.html 2>/dev/null && echo "FAIL: inspection.html still exists" || echo "PASS: deleted"</automated>
  </verify>
  <acceptance_criteria>
    - grep "/hygiene-radar" src/main/java/com/aflokkat/controller/ViewController.java returns no output
    - grep "/inspection\"" src/main/java/com/aflokkat/controller/ViewController.java returns no output
    - ls src/main/resources/templates/hygiene-radar.html exits non-zero (file does not exist)
    - ls src/main/resources/templates/inspection.html exits non-zero (file does not exist)
    - ViewController.java contains exactly 5 @GetMapping annotations (grep -c "@GetMapping" returns 5)
  </acceptance_criteria>
  <done>ViewController has 5 routes. hygiene-radar.html and inspection.html are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead endpoints from RestaurantController and verify tests pass</name>
  <files>
    src/main/java/com/aflokkat/controller/RestaurantController.java
  </files>
  <read_first>
    - src/main/java/com/aflokkat/controller/RestaurantController.java (full file — identify all endpoints before removing)
  </read_first>
  <action>
    **RestaurantController.java — remove 3 dead endpoints:**

    Remove the entire `getCuisineScores()` method (USE CASE 2 — `/cuisine-scores`):
    ```java
    /** USE CASE 2: Average inspection score per borough for a given cuisine */
    @Operation(...)
    @GetMapping("/cuisine-scores")
    public ResponseEntity<Map<String, Object>> getCuisineScores(...) { ... }
    ```

    Remove the entire `getWorstCuisines()` method (USE CASE 3 — `/worst-cuisines`):
    ```java
    /** USE CASE 3: Worst cuisines by average score in a borough */
    @Operation(...)
    @GetMapping("/worst-cuisines")
    public ResponseEntity<Map<String, Object>> getWorstCuisines(...) { ... }
    ```

    Remove the entire `getPopularCuisines()` method (USE CASE 4 — `/popular-cuisines`):
    ```java
    /** USE CASE 4: Cuisines with a minimum restaurant count */
    @Operation(...)
    @GetMapping("/popular-cuisines")
    public ResponseEntity<Map<String, Object>> getPopularCuisines(...) { ... }
    ```

    After removal, check whether any imports become unused. Remove these imports if they are
    no longer referenced by any remaining method:
    - `import com.aflokkat.aggregation.BoroughCuisineScore` — only used by getCuisineScores()
    - `import com.aflokkat.aggregation.CuisineScore` — only used by getWorstCuisines()

    Do NOT remove any other method. The following endpoints must remain:
    `/by-borough`, `/stats`, `/health`, `/hygiene-radar`, `/cuisines`, `/by-cuisine`,
    `/refresh`, `/sync-status`, `/top`, `/random`, `/{restaurantId}`, `/recent-inspections`,
    `/nearby`, `/heatmap`, `/rebuild-cache`, `/search`, `/map-points`.

    After removing the methods, run `mvn test` to catch any regressions. If any test
    references `/worst-cuisines`, `/cuisine-scores`, or `/popular-cuisines`, update that
    test to remove the call or replace it with a passing assertion for the removed endpoint.
    (Check test files with: `grep -rn "worst-cuisines\|cuisine-scores\|popular-cuisines" src/test/`)
  </action>
  <verify>
    <automated>mvn test -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "worst-cuisines\|cuisine-scores\|popular-cuisines" src/main/java/com/aflokkat/controller/RestaurantController.java returns no output
    - grep -n "getWorstCuisines\|getCuisineScores\|getPopularCuisines" src/main/java/com/aflokkat/controller/RestaurantController.java returns no output
    - mvn test exits 0 (BUILD SUCCESS)
    - grep -n "BoroughCuisineScore\|CuisineScore" src/main/java/com/aflokkat/controller/RestaurantController.java returns no output (imports cleaned up if they were only used by removed methods)
  </acceptance_criteria>
  <done>RestaurantController no longer exposes /worst-cuisines, /cuisine-scores, or /popular-cuisines. mvn test passes.</done>
</task>

</tasks>

<verification>
After both tasks:
- grep -c "@GetMapping" src/main/java/com/aflokkat/controller/ViewController.java → 5
- ls src/main/resources/templates/ → only: index.html, login.html, restaurant.html, inspection-map.html, my-bookmarks.html
- grep -n "worst-cuisines\|cuisine-scores\|popular-cuisines\|hygiene-radar\|/inspection\"" src/main/java/com/aflokkat/controller/ViewController.java src/main/java/com/aflokkat/controller/RestaurantController.java → no output
- mvn test → BUILD SUCCESS
</verification>

<success_criteria>
1. ViewController.java contains exactly 5 @GetMapping methods (/, /login, /restaurant/{id}, /inspection-map, /my-bookmarks)
2. RestaurantController.java does not contain /worst-cuisines, /cuisine-scores, or /popular-cuisines
3. templates/ directory contains exactly 5 files: index.html, login.html, restaurant.html, inspection-map.html, my-bookmarks.html
4. mvn test exits 0 with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-polish/04-02-SUMMARY.md`
</output>
