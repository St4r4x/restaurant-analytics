---
phase: 04-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/templates/index.html
  - src/main/resources/templates/login.html
  - src/main/resources/templates/restaurant.html
  - src/main/resources/templates/inspection-map.html
  - src/main/resources/templates/hygiene-radar.html
  - src/main/resources/templates/inspection.html
  - src/main/java/com/aflokkat/controller/ViewController.java
  - src/main/java/com/aflokkat/controller/RestaurantController.java
  - src/main/java/com/aflokkat/dao/RestaurantDAO.java
  - src/main/java/com/aflokkat/dao/RestaurantDAOImpl.java
  - src/main/java/com/aflokkat/service/RestaurantService.java
  - src/main/java/com/aflokkat/aggregation/BoroughCuisineScore.java
  - src/main/java/com/aflokkat/config/AppConfig.java
autonomous: true
requirements: []

must_haves:
  truths:
    - "All HTML templates have lang=\"en\" (not lang=\"fr\")"
    - "No French visible UI text remains in any template (buttons, headings, placeholders, error messages)"
    - "No French inline comments remain in any Java source file"
    - "mvn test passes with no compilation errors after the translation changes"
  artifacts:
    - path: "src/main/resources/templates/login.html"
      provides: "Login page — English UI"
      contains: "lang=\"en\""
    - path: "src/main/resources/templates/index.html"
      provides: "Dashboard — English UI"
      contains: "lang=\"en\""
    - path: "src/main/java/com/aflokkat/controller/RestaurantController.java"
      provides: "REST controller — English comments"
  key_links:
    - from: "login.html"
      to: "visible text"
      via: "placeholder, button, h1"
      pattern: "Sign in|Username|Password|Log in"
    - from: "RestaurantController.java"
      to: "inline comments"
      via: "// and /* */"
      pattern: "USE CASE [0-9]: "
---

<objective>
Translate the entire project to English: change all `lang="fr"` to `lang="en"` in HTML
templates, translate all remaining French visible UI text (buttons, headings, labels,
placeholders, error messages), translate all French inline comments and Javadoc in Java
source files.

Purpose: The project was started in French; all user-facing copy and developer comments
must be in English before the cleanup and test plans run.
Output: 7 HTML templates and ~6 Java source files with all French text replaced by English.
No IDs, class names, Thymeleaf attributes, or JS variable names are changed.
</objective>

<execution_context>
@/home/missia03/.claude/get-shit-done/workflows/execute-plan.md
@/home/missia03/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate HTML templates to English</name>
  <files>
    src/main/resources/templates/index.html,
    src/main/resources/templates/login.html,
    src/main/resources/templates/restaurant.html,
    src/main/resources/templates/inspection-map.html,
    src/main/resources/templates/hygiene-radar.html,
    src/main/resources/templates/inspection.html
  </files>
  <read_first>
    - src/main/resources/templates/index.html (full file — contains French visible text in comments and UI)
    - src/main/resources/templates/login.html (French login form text)
    - src/main/resources/templates/restaurant.html (French error text)
    - src/main/resources/templates/inspection-map.html (French status text, dropdown option)
    - src/main/resources/templates/hygiene-radar.html (possibly French, kept until plan 04-02 removes it)
    - src/main/resources/templates/inspection.html (possibly French, kept until plan 04-02 removes it)
  </read_first>
  <action>
    Apply the following changes to each file. Rules: only change visible text, HTML comments,
    and `lang` attributes — do NOT change IDs, class names, href/src values, JS variable names,
    Thymeleaf `th:` attributes, or API paths.

    **ALL templates (index.html, login.html, restaurant.html, inspection-map.html, hygiene-radar.html, inspection.html):**
    - Change `<html lang="fr">` → `<html lang="en">` on line 2 of each file

    **login.html — translate visible text:**
    - `<h1>Connexion</h1>` → `<h1>Sign In</h1>`
    - `placeholder="nom d'utilisateur"` → `placeholder="username"`
    - `placeholder="mot de passe"` → `placeholder="password"`
    - `<button id="loginBtn">Se connecter</button>` → `<button id="loginBtn">Sign In</button>`
    - `"Veuillez renseigner username et mot de passe."` → `"Please enter your username and password."`

    **index.html — translate visible text and HTML comments:**
    - Button text: `Déconnexion` → `Sign Out`
    - Section heading: `<h2>Pires Cuisines</h2>` → `<h2>Worst Cuisines</h2>`
    - HTML comment: `<!-- UC1: Restaurants par quartier -->` → `<!-- UC1: Restaurants per borough -->`
    - HTML comment: `<!-- UC5: Top 10 cuisines par nombre -->` → `<!-- UC5: Top 10 cuisines by count -->`
    - HTML comment: `<!-- UC2: Score moyen par quartier pour une cuisine -->` → `<!-- UC2: Average score per borough for a cuisine -->`
    - HTML comment: `<!-- UC3: Pires cuisines -->` → `<!-- UC3: Worst cuisines -->`
    - HTML comment: `<!-- Inspections récentes + Autour de moi -->` → `<!-- Recent inspections + Nearby -->`
    - HTML comment: `<!-- Inspections récentes -->` → `<!-- Recent inspections -->`
    - HTML comment: `<!-- Autour de moi -->` → `<!-- Nearby -->`
    - HTML comment: `<!-- UC4: Cuisines populaires -->` → `<!-- UC4: Popular cuisines -->`
    - API reference descriptions (span.api-desc text):
      - `Nombre de restaurants par quartier` → `Restaurant count per borough`
      - `Score moyen par quartier pour une cuisine` → `Average score per borough for a cuisine`
      - `Pires cuisines par score dans un quartier` → `Worst cuisines by score in a borough`
      - `Cuisines avec un minimum de restaurants` → `Cuisines with a minimum restaurant count` (if present)
      - `Restaurants des cuisines les plus risquées` → `Restaurants from the riskiest cuisines`
      - `Top N cuisines par nombre de restaurants` → `Top N cuisines by restaurant count`
      - `Tous les types de cuisine` → `All cuisine types` (if present)

    **restaurant.html — translate visible text:**
    - JS string: `` `<div class="error">Erreur: ${e.message}</div>` `` → `` `<div class="error">Error: ${e.message}</div>` ``

    **inspection-map.html — translate visible text:**
    - `<option value="">Tous les quartiers</option>` → `<option value="">All boroughs</option>`
    - `'Chargement...'` (status text, appears 2× in JS) → `'Loading...'`
    - `'Erreur de chargement'` → `'Loading error'`
    - `'Erreur réseau'` → `'Network error'`

    **hygiene-radar.html and inspection.html:** Change `lang="fr"` → `lang="en"` and translate
    any remaining French visible text found when reading the files. These files will be deleted
    in plan 04-02 but must be valid English before then.
  </action>
  <verify>
    <automated>grep -rn "lang=\"fr\"" src/main/resources/templates/ && echo "FAIL: French lang found" || echo "PASS: all lang=en"; grep -n "Connexion\|Se connecter\|mot de passe\|nom d'utilisateur\|Déconnexion\|Pires Cuisines\|Tous les quartiers\|Chargement\.\.\.\|Erreur de chargement\|Erreur réseau\|Erreur:" src/main/resources/templates/*.html && echo "WARN: French text may remain" || echo "PASS: visible French text not found"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn 'lang="fr"' src/main/resources/templates/ returns no output
    - login.html contains `Sign In` (button and h1)
    - login.html contains `placeholder="username"` and `placeholder="password"`
    - index.html contains `Sign Out` (not `Déconnexion`)
    - index.html contains `Worst Cuisines` (not `Pires Cuisines`)
    - inspection-map.html contains `All boroughs` (not `Tous les quartiers`)
    - inspection-map.html contains `'Loading...'` (not `'Chargement...'`)
    - restaurant.html contains `Error:` (not `Erreur:`)
  </acceptance_criteria>
  <done>All 6 HTML templates have lang="en" and no French visible text remains.</done>
</task>

<task type="auto">
  <name>Task 2: Translate French comments in Java source files</name>
  <files>
    src/main/java/com/aflokkat/controller/ViewController.java,
    src/main/java/com/aflokkat/controller/RestaurantController.java,
    src/main/java/com/aflokkat/dao/RestaurantDAO.java,
    src/main/java/com/aflokkat/dao/RestaurantDAOImpl.java,
    src/main/java/com/aflokkat/service/RestaurantService.java,
    src/main/java/com/aflokkat/aggregation/BoroughCuisineScore.java,
    src/main/java/com/aflokkat/config/AppConfig.java
  </files>
  <read_first>
    - src/main/java/com/aflokkat/controller/ViewController.java
    - src/main/java/com/aflokkat/controller/RestaurantController.java
    - src/main/java/com/aflokkat/dao/RestaurantDAO.java
    - src/main/java/com/aflokkat/dao/RestaurantDAOImpl.java
    - src/main/java/com/aflokkat/service/RestaurantService.java
    - src/main/java/com/aflokkat/aggregation/BoroughCuisineScore.java
    - src/main/java/com/aflokkat/config/AppConfig.java
  </read_first>
  <action>
    Translate all French comments and Javadoc in these files. Only change comment text — never
    change method signatures, class names, annotations, return types, or logic.

    **ViewController.java:**
    - Class Javadoc: `/** Vue Controller - Sert les pages HTML principales */`
      → `/** View Controller — serves the main HTML pages */`

    **RestaurantController.java:**
    - Class Javadoc: `/** REST API Controller pour l'accès aux données MongoDB */`
      → `/** REST API controller for MongoDB restaurant data */`
    - Method comment: `/** USE CASE 1: Nombre de restaurants par quartier */`
      → `/** USE CASE 1: Restaurant count per borough */`
    - Method comment: `/** USE CASE 2: Score moyen par quartier pour une cuisine donnée */`
      → `/** USE CASE 2: Average inspection score per borough for a given cuisine */`
    - Method comment: `/** USE CASE 3: Pires cuisines dans un quartier */`
      → `/** USE CASE 3: Worst cuisines by average score in a borough */`
    - Method comment: `/** USE CASE 4: Cuisines avec minimum de restaurants */`
      → `/** USE CASE 4: Cuisines with a minimum restaurant count */`
    - Method comment: `/** Statistiques générales */` → `/** Global statistics */`
    - Method comment: `/** HYGIENE RADAR: Obtient les pires restaurants avec leurs coordonnées GPS */`
      → `/** HYGIENE RADAR: Returns the worst restaurants with their GPS coordinates */`
    - Method comment: `/** Tous les types de cuisine distincts (pour alimenter les filtres) */`
      → `/** All distinct cuisine types (for populating filter dropdowns) */`
    - Method comment: `/** Top N cuisines par nombre de restaurants */`
      → `/** Top N cuisines by restaurant count */`

    **RestaurantDAO.java:**
    - `/** Retourne les statistiques par quartier */` → `/** Returns statistics per borough */`
    - `/** USE CASE 1: Compte les restaurants par quartier */` → `/** USE CASE 1: Count restaurants per borough */`
    - `/** USE CASE 2: Calcule le score moyen par quartier pour une cuisine donnée */`
      → `/** USE CASE 2: Compute average inspection score per borough for a given cuisine */`
    - `/** USE CASE 3: Retourne les pires cuisines (score moyen le plus bas) dans un quartier */`
      → `/** USE CASE 3: Return the worst cuisines (highest average score) in a borough */`
    - `/** USE CASE 3 (global): Pires cuisines tous quartiers confondus */`
      → `/** USE CASE 3 (global): Worst cuisines across all boroughs */`
    - `/** USE CASE 4: Retourne les cuisines avec un minimum de restaurants */`
      → `/** USE CASE 4: Return cuisine types with at least minCount restaurants */`

    **RestaurantDAOImpl.java:**
    - `logger.debug("Requête: Comptage des restaurants par quartier")` 
      → `logger.debug("Query: counting restaurants per borough")`
    - `logger.debug("Requête: Score moyen par quartier pour cuisine '{}'", cuisine)`
      → `logger.debug("Query: average score per borough for cuisine '{}'", cuisine)`

    **RestaurantService.java:**
    - `// =============== USE CASE 1 ===============` → keep as-is (already English enough)
    - `/** USE CASE 1: Nombre de restaurants par quartier */`
      → `/** USE CASE 1: Restaurant count per borough */`
    - `/** USE CASE 2: Score moyen par quartier pour une cuisine donnée */`
      → `/** USE CASE 2: Average inspection score per borough for a given cuisine */`
    - `/** USE CASE 3: Pires cuisines (score moyen le plus bas) dans un quartier */`
      → `/** USE CASE 3: Worst cuisines (highest average inspection score) in a borough */`
    - `/** USE CASE 4: Cuisines avec minimum de restaurants */`
      → `/** USE CASE 4: Cuisines with a minimum restaurant count */`
    - `/** Compte le nombre total de restaurants */` → `/** Returns the total restaurant count */`
    - `/** Retourne les statistiques par quartier */` → `/** Returns aggregate statistics per borough */`
    - `/** Retourne les restaurants des pires cuisines avec leurs coordonnées GPS. */`
      → `/** Returns restaurants from the worst cuisines, with GPS coordinates. */`
    - `* @param maxScore  score moyen maximum des cuisines à inclure (null = pas de filtre)`
      → `* @param maxScore  maximum average cuisine score to include (null = no filter)`
    - `// Sinon : logique "pires cuisines"` → `// Otherwise: "worst cuisines" logic`
    - `/** Retourne les N cuisines les plus représentées par nombre de restaurants */`
      → `/** Returns the top N cuisine types by restaurant count */`
    - `/** Retourne tous les types de cuisine distincts, triés alphabétiquement */`
      → `/** Returns all distinct cuisine types, sorted alphabetically */`
    - `/** Retourne un restaurant aléatoire via $sample MongoDB */`
      → `/** Returns a random restaurant via MongoDB $sample */`

    **BoroughCuisineScore.java:**
    - `return String.format("%-20s | Score moyen: %6.2f", borough, avgScore)`
      → `return String.format("%-20s | Avg score: %6.2f", borough, avgScore)`

    **AppConfig.java:**
    - `// 1. Variable d'environnement système (Docker, CI...)`
      → `// 1. System environment variable (Docker, CI...)`
  </action>
  <verify>
    <automated>mvn compile -q 2>&1 | tail -5; grep -rn "[àâéèêëïîôùûü]" src/main/java/com/aflokkat/ | grep -v ".class" | grep "//\|/\*\|\*" && echo "WARN: French accents may remain in comments" || echo "PASS: no French accented chars in comments"</automated>
  </verify>
  <acceptance_criteria>
    - mvn compile exits 0 (no compilation errors introduced)
    - ViewController.java contains `/** View Controller` (not `Vue Controller`)
    - RestaurantController.java contains `/** REST API controller for MongoDB` (not `pour l'accès`)
    - RestaurantService.java contains `Average inspection score per borough` in a Javadoc comment
    - AppConfig.java contains `// 1. System environment variable` (not `Variable d'environnement`)
    - BoroughCuisineScore.java contains `Avg score:` (not `Score moyen:`)
    - grep -rn "Requête:" src/main/java/ returns no output
  </acceptance_criteria>
  <done>All French comments and Javadoc in Java source files replaced with English equivalents. mvn compile passes.</done>
</task>

</tasks>

<verification>
After both tasks:
- grep -rn 'lang="fr"' src/main/resources/templates/ → no output
- grep -rn "[àâéèêëïîôùûüÀÂÉÈÊËÏÎÔÙÛÜ]" src/main/java/com/aflokkat/ | grep -v ".class" → no accented chars remain in Java comments
- mvn test -q → BUILD SUCCESS (no regressions)
</verification>

<success_criteria>
1. Every HTML template has lang="en"
2. No French visible text remains in any rendered template (login, dashboard, restaurant, inspection-map)
3. No French inline comments or Javadoc remain in any Java source file
4. mvn test passes with no failures introduced by translation
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-polish/04-01-SUMMARY.md`
</output>
