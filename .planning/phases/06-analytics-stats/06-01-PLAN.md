---
phase: 06-analytics-stats
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/test/java/com/aflokkat/controller/AnalyticsControllerTest.java
  - src/test/java/com/aflokkat/controller/ViewControllerAnalyticsTest.java
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04

must_haves:
  truths:
    - "The full test suite compiles and passes with the two new test files present"
    - "AnalyticsControllerTest contains four @Disabled test methods that will be enabled in Plan 02"
    - "ViewControllerAnalyticsTest contains one @Disabled test method that will be enabled in Plan 03"
  artifacts:
    - path: "src/test/java/com/aflokkat/controller/AnalyticsControllerTest.java"
      provides: "Wave 0 test scaffold for STAT-01 through STAT-04"
      exports: ["testKpis_returns200", "testBoroughGrades_returns5Boroughs", "testCuisineRankings_returnsTwoLists", "testAtRisk_returnsEntries"]
    - path: "src/test/java/com/aflokkat/controller/ViewControllerAnalyticsTest.java"
      provides: "Wave 0 test scaffold for STAT-04 (public page load)"
      exports: ["testAnalyticsPage_returns200"]
  key_links:
    - from: "AnalyticsControllerTest"
      to: "AnalyticsController (not yet created)"
      via: "@InjectMocks + standaloneSetup"
      pattern: "MockMvcBuilders.standaloneSetup\\(analyticsController\\)"
    - from: "ViewControllerAnalyticsTest"
      to: "ViewController.analytics()"
      via: "direct method call assertion"
      pattern: "viewController.analytics\\(\\)"
---

<objective>
Create Wave 0 test scaffolds so the test suite compiles and all existing tests remain green before any analytics implementation exists.

Purpose: Nyquist compliance — every implementation task in Wave 1 and Wave 2 must have a pre-existing automated verify command. These test files are the contracts that will be unlocked when their corresponding implementation lands.
Output: Two test files with @Disabled stubs; `mvn test` passes.
</objective>

<execution_context>
@/home/missia03/.claude/get-shit-done/workflows/execute-plan.md
@/home/missia03/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-analytics-stats/06-VALIDATION.md

@src/test/java/com/aflokkat/controller/RestaurantControllerSearchTest.java
@src/test/java/com/aflokkat/controller/ViewControllerDashboardTest.java
</context>

<interfaces>
<!-- Patterns to replicate exactly. Extracted from codebase. -->

From RestaurantControllerSearchTest.java (the canonical Wave 0 scaffold pattern):
```java
@ExtendWith(MockitoExtension.class)
class RestaurantControllerSearchTest {
    @Mock private RestaurantDAO restaurantDAO;
    @InjectMocks private RestaurantController restaurantController;
    private MockMvc mockMvc;

    @BeforeEach
    void setUp() {
        mockMvc = MockMvcBuilders.standaloneSetup(restaurantController).build();
    }
    // @Test methods follow
}
```

From ViewControllerDashboardTest.java (direct method call pattern for ViewController):
```java
@ExtendWith(MockitoExtension.class)
public class ViewControllerDashboardTest {
    @InjectMocks private ViewController viewController;

    @Test
    public void index_returnsIndex_forAnonymous() {
        assertEquals("index", viewController.index(null));
    }
}
```

AnalyticsController class (does not exist yet — declare @Mock for its service dep):
- Will be at: com.aflokkat.controller.AnalyticsController
- Will depend on: RestaurantService (use @Mock)
- Will use: @InjectMocks AnalyticsController analyticsController

Key import for @Disabled: org.junit.jupiter.api.Disabled
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnalyticsControllerTest — Wave 0 scaffold</name>
  <files>src/test/java/com/aflokkat/controller/AnalyticsControllerTest.java</files>
  <read_first>
    - src/test/java/com/aflokkat/controller/RestaurantControllerSearchTest.java (canonical scaffold pattern — replicate structure exactly)
    - src/main/java/com/aflokkat/service/RestaurantService.java (the mock target — verify class name and package)
  </read_first>
  <behavior>
    - testKpis_returns200: @Disabled, performs GET /api/analytics/kpis, expects 200 + JSON keys totalRestaurants, percentGradeA, avgScore, atRiskCount
    - testBoroughGrades_returns5Boroughs: @Disabled, performs GET /api/analytics/borough-grades, expects 200 + data array
    - testCuisineRankings_returnsTwoLists: @Disabled, performs GET /api/analytics/cuisine-rankings, expects 200 + best and worst arrays each size 10
    - testAtRisk_returnsEntries: @Disabled, performs GET /api/analytics/at-risk, expects 200 + data array with entries having restaurantId, name, borough, lastGrade
  </behavior>
  <action>
Create `src/test/java/com/aflokkat/controller/AnalyticsControllerTest.java` with the following exact structure:

```
package com.aflokkat.controller;

import com.aflokkat.aggregation.CuisineScore;
import com.aflokkat.dto.AtRiskEntry;
import com.aflokkat.service.RestaurantService;
import org.bson.Document;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import java.util.Arrays;
import java.util.Collections;

import static org.mockito.Mockito.when;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/**
 * Wave 0 test scaffold for STAT-01 through STAT-04.
 * All tests are @Disabled until AnalyticsController is created in Plan 06-02.
 * Pattern: @ExtendWith(MockitoExtension.class) + standaloneSetup — NEVER @WebMvcTest.
 */
@ExtendWith(MockitoExtension.class)
class AnalyticsControllerTest {

    @Mock
    private RestaurantService restaurantService;

    @InjectMocks
    private AnalyticsController analyticsController;

    private MockMvc mockMvc;

    @BeforeEach
    void setUp() {
        mockMvc = MockMvcBuilders.standaloneSetup(analyticsController).build();
    }

    /**
     * STAT-01: GET /api/analytics/kpis returns 200 with all four KPI fields.
     */
    @Test
    @Disabled("Wave 0 stub — enable when AnalyticsController is created in Plan 06-02")
    void testKpis_returns200() throws Exception {
        when(restaurantService.countAll()).thenReturn(27000L);

        mockMvc.perform(get("/api/analytics/kpis"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.totalRestaurants").exists())
                .andExpect(jsonPath("$.percentGradeA").exists())
                .andExpect(jsonPath("$.avgScore").exists())
                .andExpect(jsonPath("$.atRiskCount").exists());
    }

    /**
     * STAT-02: GET /api/analytics/borough-grades returns 200 with borough data.
     */
    @Test
    @Disabled("Wave 0 stub — enable when AnalyticsController is created in Plan 06-02")
    void testBoroughGrades_returns5Boroughs() throws Exception {
        mockMvc.perform(get("/api/analytics/borough-grades"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.data").isArray());
    }

    /**
     * STAT-03: GET /api/analytics/cuisine-rankings returns two ranked lists of 10.
     */
    @Test
    @Disabled("Wave 0 stub — enable when AnalyticsController is created in Plan 06-02")
    void testCuisineRankings_returnsTwoLists() throws Exception {
        CuisineScore cs = new CuisineScore("Italian", 12.4, 500);
        when(restaurantService.getWorstCuisinesByAverageScore(10))
                .thenReturn(Collections.nCopies(10, cs));
        when(restaurantService.getBestCuisinesByAverageScore(10))
                .thenReturn(Collections.nCopies(10, cs));

        mockMvc.perform(get("/api/analytics/cuisine-rankings"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.best").isArray())
                .andExpect(jsonPath("$.worst").isArray());
    }

    /**
     * STAT-04: GET /api/analytics/at-risk returns 200 with data array containing entries.
     */
    @Test
    @Disabled("Wave 0 stub — enable when AnalyticsController is created in Plan 06-02")
    void testAtRisk_returnsEntries() throws Exception {
        AtRiskEntry entry = new AtRiskEntry();
        entry.setRestaurantId("12345");
        entry.setName("Dirty Diner");
        entry.setBorough("MANHATTAN");
        entry.setLastGrade("C");

        when(restaurantService.getAtRiskRestaurants(null, 50))
                .thenReturn(Collections.singletonList(entry));

        mockMvc.perform(get("/api/analytics/at-risk"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.data").isArray())
                .andExpect(jsonPath("$.data[0].restaurantId").value("12345"))
                .andExpect(jsonPath("$.data[0].name").value("Dirty Diner"))
                .andExpect(jsonPath("$.data[0].borough").value("MANHATTAN"))
                .andExpect(jsonPath("$.data[0].lastGrade").value("C"));
    }
}
```

Note: `AnalyticsController` does not exist yet. The `@InjectMocks` line will cause a compile error until the class is created. To prevent this, create a **minimal placeholder class** alongside the test (or accept that `mvn test` will fail to compile until Plan 02). The correct approach: create a bare-minimum placeholder `src/main/java/com/aflokkat/controller/AnalyticsController.java` with just the class declaration and @Autowired RestaurantService field — no endpoints yet. This lets the test file compile and @Disabled tests are skipped.

Also note: `restaurantService.getWorstCuisinesByAverageScore(10)` and `restaurantService.getBestCuisinesByAverageScore(10)` — the first method exists; the second does not yet. Since the test is @Disabled, the stub `when(...)` calls will not execute at test time. The method reference must compile though — so `getBestCuisinesByAverageScore` must exist on RestaurantService as a stub returning null or empty list. Add a stub method to RestaurantService: `public List&lt;CuisineScore&gt; getBestCuisinesByAverageScore(int limit) { return java.util.Collections.emptyList(); }` — this will be replaced by real implementation in Plan 02.
  </action>
  <verify>
    <automated>cd /home/missia03/Aflokkat/big_data/quickstart-app && mvn test -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `src/test/java/com/aflokkat/controller/AnalyticsControllerTest.java` exists
    - File contains exactly 4 @Test @Disabled methods: testKpis_returns200, testBoroughGrades_returns5Boroughs, testCuisineRankings_returnsTwoLists, testAtRisk_returnsEntries
    - `mvn test` passes (disabled tests are skipped, not failed)
    - `grep -c "@Disabled" src/test/java/com/aflokkat/controller/AnalyticsControllerTest.java` returns 4
  </done>
</task>

<task type="auto">
  <name>Task 2: ViewControllerAnalyticsTest — Wave 0 scaffold</name>
  <files>src/test/java/com/aflokkat/controller/ViewControllerAnalyticsTest.java</files>
  <read_first>
    - src/test/java/com/aflokkat/controller/ViewControllerDashboardTest.java (canonical ViewController test pattern)
    - src/main/java/com/aflokkat/controller/ViewController.java (verify method signature for analytics() — does not exist yet)
  </read_first>
  <action>
Create `src/test/java/com/aflokkat/controller/ViewControllerAnalyticsTest.java`:

```java
package com.aflokkat.controller;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * Wave 0 test scaffold for STAT-04 public page accessibility.
 * Test is @Disabled until ViewController.analytics() is added in Plan 06-03.
 */
@ExtendWith(MockitoExtension.class)
public class ViewControllerAnalyticsTest {

    @InjectMocks
    private ViewController viewController;

    /**
     * STAT-04: GET /analytics returns the "analytics" view (fully public — no auth required).
     */
    @Test
    @Disabled("Wave 0 stub — enable when ViewController.analytics() is added in Plan 06-03")
    public void testAnalyticsPage_returns200() {
        assertEquals("analytics", viewController.analytics());
    }
}
```

Note: `ViewController.analytics()` does not exist yet. Add a stub to `ViewController.java`:
```java
@GetMapping("/analytics")
public String analytics() {
    return "analytics";
}
```
This stub serves as the route registration even before the template exists. The Thymeleaf template is created in Plan 03. Spring will return 500 if the template is missing but the route is registered — the unit test calls the method directly (no template resolution), so the @Disabled test file will compile and pass at the framework level once the method exists.
  </action>
  <verify>
    <automated>cd /home/missia03/Aflokkat/big_data/quickstart-app && mvn test -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `src/test/java/com/aflokkat/controller/ViewControllerAnalyticsTest.java` exists
    - File contains 1 @Test @Disabled method: testAnalyticsPage_returns200
    - `ViewController.java` contains `@GetMapping("/analytics") public String analytics() { return "analytics"; }`
    - `mvn test` passes — all existing tests green, Wave 0 stubs skipped
    - `grep -c "@Disabled" src/test/java/com/aflokkat/controller/ViewControllerAnalyticsTest.java` returns 1
  </done>
</task>

</tasks>

<verification>
Full suite must be green after both tasks:

```bash
cd /home/missia03/Aflokkat/big_data/quickstart-app && mvn test
```

All previously passing tests still pass. New @Disabled tests show as skipped (not failed). Compilation succeeds for both new test files.
</verification>

<success_criteria>
- `mvn test` exits 0
- 2 new test files created under `src/test/java/com/aflokkat/controller/`
- 5 total new @Disabled stub methods (4 in AnalyticsControllerTest, 1 in ViewControllerAnalyticsTest)
- Minimal placeholder `AnalyticsController.java` exists with class declaration + @Autowired RestaurantService field
- `ViewController.java` has `analytics()` stub method returning `"analytics"`
- Stub `getBestCuisinesByAverageScore(int limit)` on RestaurantService returns empty list
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-stats/06-01-SUMMARY.md`
</output>
