<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.4</version>
    <relativePath/>
  </parent>
  <groupId>com.st4r4x</groupId>
  <artifactId>restaurant-hygiene-app</artifactId>
  <packaging>jar</packaging>
  <version>2.0</version>
  <name>Restaurant Hygiene Control App</name>
  <properties>
    <java.version>25</java.version>
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Override Byte Buddy from Spring Boot BOM — 1.17.x adds full Java 25 class file support.
         Spring Boot 3.4.4 BOM resolves 1.15.11 which cannot instrument classes under Java 25
         (InlineDelegateByteBuddyMockMaker.createMockType throws MockitoException for any mock). -->
    <byte-buddy.version>1.17.5</byte-buddy.version>
  </properties>
  <dependencies>
    <!-- Spring Boot Web Starter -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Thymeleaf Template Engine -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>

    <!-- MongoDB Driver -->
    <dependency>
      <groupId>org.mongodb</groupId>
      <artifactId>mongodb-driver-sync</artifactId>
    </dependency>

    <!-- PostgreSQL + JPA -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Environment Configuration -->
    <dependency>
      <groupId>io.github.cdimascio</groupId>
      <artifactId>dotenv-java</artifactId>
      <version>3.0.0</version>
    </dependency>

    <!-- Redis (Phase 2) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>

    <!-- API Documentation -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.8.3</version>
    </dependency>

    <!-- Security -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <!-- JWT -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>0.11.5</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>0.11.5</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>0.11.5</version>
      <scope>runtime</scope>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-logging</artifactId>
    </dependency>

    <!-- logstash-logback-encoder — JSON structured log output for prod profile -->
    <!-- Spring Boot 3.4 ships Logback 1.5.x — compatible with encoder 8.x    -->
    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>8.0</version>
    </dependency>

    <!-- Rate limiting — Bucket4j 8.x requires Java 17+ -->
    <dependency>
      <groupId>com.bucket4j</groupId>
      <artifactId>bucket4j-core</artifactId>
      <version>8.10.1</version>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Spring Security Test: SecurityMockMvcRequestPostProcessors.authentication() etc. -->
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <!-- 5.x required for Java 21+ Byte Buddy instrumentation support -->
      <version>5.17.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <version>5.17.0</version>
      <scope>test</scope>
    </dependency>
    <!-- mockito-inline removed: Mockito 5.x merged inline mocking into mockito-core.
         Having both on the classpath causes MockMaker conflicts on Java 21+. -->

    <!-- JUnit Vintage Engine: allows JUnit 4 @Test annotations to run on the JUnit 5 platform.
         Required because existing tests (AppConfigTest, AuthServiceTest, etc.) use JUnit 4. -->
    <dependency>
      <groupId>org.junit.vintage</groupId>
      <artifactId>junit-vintage-engine</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <!-- Spring Boot Maven Plugin -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- Java Compiler Plugin -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
        <configuration>
          <source>25</source>
          <target>25</target>
          <encoding>UTF-8</encoding>
        </configuration>
      </plugin>

      <!-- Surefire Plugin: allow Mockito's byte-buddy-agent to attach dynamically on Java 21+.
           Without -XX:+EnableDynamicAgentLoading, Mockito self-attachment on Java 25 causes
           a StackOverflowError in @WebMvcTest slice tests. -->
      <!-- @{argLine} — Surefire late-binding: reads the argLine property at execution time,
           AFTER jacoco:prepare-agent has deposited -javaagent:... into it during the initialize phase.
           Using ${argLine} (dollar-sign) evaluates at POM parse time — before prepare-agent runs —
           and picks up an empty string, causing JaCoCo agent to be silently dropped.
           This would cause StackOverflowError in all Mockito ByteBuddy-instrumented tests. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
        </configuration>
      </plugin>

      <!-- JaCoCo Maven Plugin: coverage instrumentation, HTML report, and threshold check.
           Version must be explicit — Spring Boot 3.4.4 BOM does not manage jacoco-maven-plugin.
           0.8.14+ required for official Java 25 support (0.8.13 = experimental). -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.14</version>
        <executions>

          <!-- Goal 1: Deposit -javaagent:jacocoagent.jar=... into the 'argLine' Maven property.
               Must run at 'initialize' phase — before test-compile — so Surefire picks it up
               via @{argLine} late-binding. Running at any later phase causes Surefire to miss it. -->
          <execution>
            <id>jacoco-prepare-agent</id>
            <phase>initialize</phase>
            <goals><goal>prepare-agent</goal></goals>
          </execution>

          <!-- Goal 2: Generate HTML/XML report at target/site/jacoco/ after tests complete.
               Excludes: pure data carriers (DTOs, JPA entities, aggregation POJOs, MongoDB domain POJOs).
               Covered: service, dao, controller, security, cache, sync, util, config (D-04, D-05). -->
          <execution>
            <id>jacoco-report</id>
            <phase>test</phase>
            <goals><goal>report</goal></goals>
            <configuration>
              <excludes>
                <!-- Exclusion format: forward-slash paths (NOT dot-notation). 'com.st4r4x.dto.*' would NOT work. -->
                <exclude>com/st4r4x/dto/**</exclude>
                <exclude>com/st4r4x/entity/**</exclude>
                <exclude>com/st4r4x/aggregation/**</exclude>
                <exclude>com/st4r4x/domain/**</exclude>
              </excludes>
            </configuration>
          </execution>

          <!-- Goal 3: Fail build if INSTRUCTION coverage drops below threshold.
               Bound to 'test' phase (same as report) so plain 'mvn test' enforces it.
               Binding to 'verify' would require 'mvn verify' — would silently pass on 'mvn test'. -->
          <execution>
            <id>jacoco-check</id>
            <phase>test</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <excludes>
                <!-- Same exclusion set as jacoco-report — D-04 -->
                <exclude>com/st4r4x/dto/**</exclude>
                <exclude>com/st4r4x/entity/**</exclude>
                <exclude>com/st4r4x/aggregation/**</exclude>
                <exclude>com/st4r4x/domain/**</exclude>
              </excludes>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <!-- Measured baseline: 43% — threshold set at 38% (baseline minus 5%) -->
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.38</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>

        </executions>
      </plugin>

      <!-- Maven Failsafe Plugin: integration test runner for *IT.java files.
           No version: Spring Boot 3.4.4 BOM manages at 3.5.2.
           No *IT.java files exist yet — Failsafe runs zero tests; exits 0.
           argLine uses same @{argLine} late-binding as Surefire (D-06) —
           required for Phase 14 Testcontainers tests to receive JaCoCo agent.
           D-08: bound to integration-test + verify lifecycle phases. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <!-- No <version>: Spring Boot 3.4.4 BOM manages at 3.5.2 -->
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
